# TODO — task history

Append-only log of non-trivial tasks and their status. Add a task when you start
it, flip it to **In progress**, then **Done** when finished — never delete
completed entries (this is the history). Each line: `YYYY-MM-DD — description`.
Status markers: `- [ ]` pending · `- [~]` in progress · `- [x]` done ·
`- [-]` cancelled/blocked (note why). Newest first within each group.

## In progress

_(none)_

## Pending

Next steps follow the MVP milestones in `docs/PRD.md` (§5, §14).

The §6.2.1 operator-catalogue roadmap (P0 → P1 → P2) is **complete** (catalogue → 18). All listed MVP/v1.1 items are now done. The §6.2.2 lakehouse roadmap (P0 → P1 → P2 → third-party P3) is **complete** (catalogue → 44); the only remaining v1.3 item is the env-gated **live-deploy on `3.0.2`** (Airflow isn't installed in this Jupyter env).

**v1.3 — "lakehouse breadth & UX polish" (specced + 3 tracks implemented 2026-06-22; `docs/PRD.md` §5/§6.2.2/§6.6/§6.1.3/§6.8/§15).** The user chose to **keep Traditional** (no removal track).

- [x] **Field info bubbles** (§6.1.3 / §15.1 / §15.3) — DONE 2026-06-22 (see Done entry below).
- [x] **Friendly log viewer** (§6.6 / §15.9) — DONE 2026-06-22 (client-side level classification; no server-contract change — see Done entry).
- [x] **Lakehouse operators P0–P2** (§6.2.2) — **DONE 2026-06-22** (24 ops total, catalogue → 42; wheel-verified, registry+codegen tests). Only third-party **P3** (Great Expectations / OpenMetadata) remains, which also needs the §13 Q13 gating extension. **Do NOT** add per-DB SQL ops (§13 Q14). Each YAML ships BOTH `template_taskflow` and `template_traditional` (Traditional stays — §6.3).
- [ ] **Live-deploy the P0–P2 lakehouse ops** on the devcontainer `apache/airflow:3.0.2` (install the providers; deploy a DAG using each; confirm registration + the gate doesn't false-block; against a standard-only index confirm `missing-provider` + the pip hint). The one verification step the Jupyter env couldn't run — Airflow + the providers are **not installed here** (wheel-inspection stood in for it).
- [x] **Notifications & callbacks** (§6.8 / §15.14) — **DONE** (DAG-level 2026-06-22 + per-task `node.callbacks` & `on_retry` 2026-06-22; 5-channel notifier registry [Smtp/Slack/Apprise/Discord/Opsgenie]; NOTIFY tab + NODE-tab Notifications section + `on_*_callback` codegen + deploy-time provider hard-gate; see Done entries). (No TelegramNotifier — the telegram provider ships no notifications module.) Niche `on_execute`/`on_skipped` task events remain optional 📝 (addable with no IR change).
- [x] **Third-party P3 ops** (§6.2.2) — Great Expectations + OpenMetadata + the §13 Q13 gating extension — **DONE 2026-06-23** (see Done entry below). Catalogue → 44.

## Done

- [x] 2026-06-23 — **S3 deploy target (v1.2; PRD §6.5.1 / §8.7) — completes the "Git + S3 DeployTarget" line item.** The object-storage `DeployTarget`, riding the abstraction the Git target just established. **S3 API shapes verified against the authoritative botocore service model** (botocore 1.43.36, gzip+json): `PutObject(Bucket,Key,Body)`, `GetObject→Body`, `ListObjectsV2(Bucket,Prefix)→Contents[].Key` + `IsTruncated`/`NextContinuationToken` (paginated), `DeleteObject`, `HeadObject` (404 → ClientError) — since boto3 isn't installed here. Introduced a thin **`DeployTarget` base** (the PRD §6.5.1 interface) and reparented `SharedVolumeTarget(DeployTarget)`; the 8 deploy-function hints + the factory return now type against `DeployTarget` (no behavior change). **`S3DeployTarget(DeployTarget)`** is standalone (object storage, not a filesystem) but reuses the shared namespacing helpers (`_FILENAME_RE` key safety, `_parse_header` provenance, `MANAGED_PREFIX` collision guard, `_AIRFLOWIGNORE_PATTERNS`, `_BACKUP_SUFFIX`): `write`=`put_object` (collision-guarded; prior managed object copied to a `…py.bak` object), `read`/`exists`=`get_object`/`head_object` (404→not-found via a botocore-shaped `.response` check, no botocore import needed), `delete`=`delete_object` (+ the backup), `list`=**paginated** `ListObjectsV2` filtered to managed `.py` directly under the prefix (skips nested/non-`.py`/unmanaged), `verify`/`has_backup`/`restore_backup`, `ensure_airflowignore`=get-modify-put on the `.airflowignore` object. boto3 is **lazily imported** (module loads without it) and the client is **injectable** (tests run against a faithful in-memory fake matching the verified botocore shapes, incl. pagination). Path is `s3://<bucket>/<prefix>/<dag_id>.py`. **Factory** `get_deploy_target()` extended (`AIRFLOW_DEPLOY_TARGET=s3`); **config** (`config.py`) documents `AIRFLOW_S3_DAGS_BUCKET`/`PREFIX`/`ENDPOINT_URL` (MinIO/S3-compatible). consistency=`eventual` (bundle discovery is delayed regardless of S3's own strong consistency). **Server-only**; the frontend deploy flow is target-agnostic. **Tests:** +10 `test_deploy.py` (put+provenance+path; overwrite→backup→rollback; collision refusal; paginated list skipping nested/non-py/unmanaged; delete removes object+backup; idempotent ensure_airflowignore; unsafe-filename rejection; requires-a-bucket; factory; deploy_dag end-to-end through S3 — all against the fake client). PRD §6.5.1 + §8.7 + §5 v1.2 + §14 v1.2 row + `config.py` updated. **Remaining (env-gated, like the shared-volume + Git live-deploys):** an end-to-end against a real MinIO/S3 bucket + an Airflow S3 DAG bundle — not runnable here (no boto3 / S3 / Airflow); the put/get/list/delete logic is verified against a faithful botocore-shaped fake + the API verified against the botocore model, which stand in. Then ran a **2-dimension adversarial review** (S3-API-correctness-vs-botocore-model / interface-parity+safety; verifiers reproducing against the model + fake clients) → **2 findings, both confirmed LOW (0 refuted; no HIGH — the target reused proven patterns + the API was model-verified), both fixed:** (LOW) `_is_not_found` treated **`NoSuchBucket` (HTTP 404) as a missing key**, so `delete`/`purge` against a mistyped `AIRFLOW_S3_DAGS_BUCKET` silently reported success → now `NoSuchBucket` is excluded so the config error surfaces (GET/DELETE return the code; a HEAD on a missing bucket only 404s — an inherent S3 limitation, noted); (LOW) `list()` caught `except Exception` per object (broader than SharedVolume's OSError-only skip), silently dropping a managed DAG on any read error → narrowed to skip only a not-found (object vanished mid-list) and **propagate** unexpected errors (auth/decode), matching the shared-volume contract. +2 regression tests (NoSuchBucket surfaces; list skips a vanished object but propagates a 403). Re-verified: **180 server pytest** (168 baseline + 12 S3), frontend untouched (no TS/CSS → tsc/jest/eslint/prettier/stylelint/dev-build unaffected).

- [x] 2026-06-23 — **Git deploy target (v1.2; PRD §6.5.1 / §8.7).** The first alternative `DeployTarget` beyond the shared volume (user-picked the more self-contained, locally-verifiable infra item; per-user identity+audit + S3 remain). **`GitDeployTarget` subclasses `SharedVolumeTarget`** (reusing namespacing, atomic write, the provenance-header collision guard, backup/rollback, `.airflowignore` — PRD §8.7 "SharedVolumeTarget owns namespacing so Git/S3 reuse it"); `root = <AIRFLOW_GIT_DAGS_REPO>/<AIRFLOW_GIT_DAGS_SUBDIR>` (default subdir `dags`). Each mutating op (`write`/`delete`/`restore_backup`) does the inherited file op, then `git add` the `.py` (+ `.airflowignore`) and `commit` (with a fixed `airflow-studio@localhost` identity via `-c`, so it doesn't depend on the server's git config), then `push` to `AIRFLOW_GIT_DAGS_REMOTE`/`AIRFLOW_GIT_DAGS_BRANCH` when a remote is set — mapping to an Airflow `GitDagBundle`. `_ensure_root` validates the repo (`git rev-parse --is-inside-work-tree` → actionable `DeployError` when unset/not-a-repo); the `.bak` backup stays **untracked** (only `.py`+`.airflowignore` staged); empty commits skipped (`diff --cached --quiet`); every git call is **60 s-timeout-bounded** + OSError-guarded (an unreachable remote / missing git can't hang the executor thread). New **`get_deploy_target()` factory** keyed on `AIRFLOW_DEPLOY_TARGET` (`shared_volume` default · `git`); the 8 deploy functions (`deploy_dag`/`rollback_dag`/`purge_dag`/`rename_preflight`/`retire_old_dag`/`find_orphans`/`find_source_path`/`is_drifted`) now default to it, so the handlers (which pass no target) use the configured backend with **zero handler change**. **Server-only** — the frontend deploy flow + UI are target-agnostic (the `POST deploy` contract is unchanged). Config documented in `config.py`. **Tests:** +9 `test_deploy.py` against a **real local git repo** (commit-with-provenance + working-tree `list`; push to a `--bare` remote; overwrite→untracked `.bak`→rollback-commits; delete-commits-removal + gone-from-HEAD; requires-a-repo + rejects-non-git-dir errors; inherited collision safety; factory selection; `deploy_dag` end-to-end through the git target). PRD §6.5.1 + §8.7 + §5 v1.2 + §14 v1.2 row + `config.py` updated. **Remaining (env-gated, like the shared-volume live-deploy):** an end-to-end against a running Airflow configured with a GitDagBundle pointing at the repo — not runnable here (no Airflow); the write/commit/push logic is verified against a real git repo, which stands in (same bar as every prior deploy feature). Then ran a **2-dimension adversarial review** (git-correctness+robustness / interface-parity+security; 8 agents, verifiers reproducing against real repos) → **6 findings, all confirmed (4 HIGH · 1 MED · 1 LOW; 0 refuted), 3 distinct root causes, all fixed:** (HIGH) `push HEAD:<branch>` **cross-wired branches** — a commit on the working tree's current branch was pushed onto a *different* configured branch, silently diverging the bundle branch → now `_ensure_root` **requires HEAD == the configured branch** (detached/other → actionable `DeployError`, no mutation) and push uses an explicit `refs/heads/<b>:refs/heads/<b>` refspec; (HIGH/MED, two reviewers) `git commit` with **no pathspec committed the entire index** — unrelated/secret pre-staged files would leak into `airflow-studio:` commits + the empty-check was repo-wide → both the empty-check and the commit are now **pathspec-scoped** (`diff --cached --quiet -- <paths>` + `commit -m … -- <paths>`), so a deploy commits *only* the files it touched (verified: a pre-staged `secret.env` is NOT swept in); (HIGH) a **rejected push left an un-rolled-back local commit** and retries stacked commits → push is now **transactional** (on failure, `reset --soft HEAD~1` undoes the commit so a retry starts clean, with an actionable fetch/rebase error); (LOW) the rollback `.bak` polluted `git status` → a `.gitignore` (`*.bak`, `.afdag-tmp-*`) is ensured + committed. +4 regression tests (wrong-branch refusal, path-scoped commit excludes a pre-staged secret, push-failure rollback + no-stacking, `.gitignore`). Re-verified: **168 server pytest** (155 baseline + 13 git), frontend untouched (no TS/CSS change → tsc/jest/eslint/prettier/stylelint/dev-build unaffected).

- [x] 2026-06-23 — **Advanced asset scheduling — any/all + asset-or-time (v1.2; §6.9).** Extended the §6.9 asset scheduling with the deferred boolean/combined surface (user-picked the fully-verifiable v1.2 track). **Wheel-verified against the EXACT 3.0.2 target** (airflow-core **3.0.2** + task-sdk 1.2.2, zipfile+ast): `AssetAny`/`AssetAll(*objects)` import from `airflow.sdk`; `AssetOrTimeSchedule(*, timetable, assets)` from `airflow.timetables.assets` (FOUND in 3.0.2); `CronTriggerTimetable(cron, *, timezone, …)` from `airflow.timetables.trigger` (FOUND in 3.0.2) — **`timezone` is required** (→ emit `timezone='UTC'`). **Catch:** `CronTriggerTimetable` normalizes presets via `cron_presets` (so `@daily` etc. combine fine) but **`@once` is not a cron** and would raise at parse → the combined mode is **guarded** against `@once`/empty (falls back to asset-override). **IR:** `dag.schedule_asset_mode: 'all'|'any'` (default all) + `dag.schedule_with_time: boolean`. **Codegen** (`codegen.py`): new `_build_schedule(dag) -> (value_expr, imports_set)` centralizes the schedule decision — `all` → bare `[Asset(...)]` list (Airflow coerces to AssetAll), `any` → `AssetAny(...)`, combine+cron → `AssetOrTimeSchedule(timetable=CronTriggerTimetable(cron, timezone='UTC'), assets=AssetAll/AssetAny(...))`, else override; `_dag_args` now calls it; **refactored `_collect_imports`** from a `uses_assets: bool` to an `extra_imports: tuple` (the schedule's required asset/timetable lines ∪ `Asset` for node inlets/outlets) — collected **only when used** (a no-asset DAG is byte-unchanged). **Frontend:** `ir.ts` (+ the two dag fields), `forms.ts` (DAG-form **match-mode** enum select `all`/`any` + **"also run on time"** boolean, ordered after `schedule_assets`; round-tripped in `dagToFormData`/`formDataToDag` with an unknown-mode→`all` coercion). **Tests:** +3 `test_codegen.py` (any vs all; AssetOrTimeSchedule both modes + the timetable imports; @once/no-cron fallback), +1 `forms.spec.ts` (mode/combine round-trip + enum/boolean schema). Verified: **155 server pytest** (was 152: +3), **85 jest** (was 84: +1), tsc/eslint/prettier/stylelint 0, dev build OK. PRD §6.9 (any/all + or-time shipped; deferred list trimmed to AssetAlias/watchers/group/extra/@asset/nested-conditions) + §5 v1.2 + §15.1 (DAG match-mode/combine fields, prose + ASCII) updated. Then ran a **2-dimension adversarial review** (wheel+codegen / design-semantics+frontend; reviewers + per-finding verifier against the **airflow-core 3.0.2** wheel) → **0 code defects; 1 confirmed (LOW, doc-cosmetic) + 1 refuted (doc nit), both doc-only, both applied.** Verifiers confirmed at the wheel/render level: the 4 import paths + signatures are exact for 3.0.2, `AssetOrTimeSchedule.__init__` is keyword-only (`*, timetable, assets`), every mode renders valid runnable Python, imports are collected only-when-used, and the `@once`/`@continuous` fallback is correct (a `@continuous` combined schedule renders an asset-only `schedule=[Asset(...)]` with NO unused timetable import). Fixes: (LOW confirmed) the §5 v1.2 shorthand wrote `AssetOrTimeSchedule(CronTriggerTimetable(...), …)` without the keyword-only `timetable=` (the canonical §6.9 spec + codegen were already correct) → added `timetable=`; (refuted but valid) the `schedule_with_time` help text + §6.9 listed only `@once`/no-schedule as non-combinable but the guard also covers `@continuous` (reachable via the custom-cron free-text input) → added `@continuous` to both. No code change needed.

- [x] 2026-06-23 — **Asset / data-aware scheduling (v1.2; new PRD §6.9).** Airflow 3 Assets (formerly Datasets): schedule a DAG on asset updates + declare per-task produce/consume. **Wheel-verified** against `apache-airflow-task-sdk==1.2.2` (zipfile+ast): `Asset` is exported from **`airflow.sdk`** (`from airflow.sdk import Asset`); `Asset(name=None, uri=None, …)` requires at least one and a lone positional sets **both** name and uri (so a single string works for a plain name OR a URI); `BaseOperator` (`airflow/sdk/bases/operator.py`) accepts `inlets`/`outlets` kwargs and `@task` forwards them (same path the per-task callbacks use). **IR (data-only, no new graph topology — assets aren't nodes/edges, so cycle-check is untouched):** `dag.schedule_assets: string[]` + per-node `inlets`/`outlets: string[]` (all optional, absent on older `.afdag`). **Codegen** (`codegen.py`): `_asset_list_expr` renders `[Asset('a'), …]`; `_dag_args` emits `schedule=[Asset(...)]` when `schedule_assets` is non-empty (**overriding** the cron/preset schedule); `_node_assets` merges `inlets`/`outlets=[Asset(...)]` into the node's `common` trailing-kwargs slot (so every operator template emits them with no per-operator edit — `@task(…)` decorator for native ops, operator call otherwise, both families); `from airflow.sdk import Asset` collected **only when** an asset is actually used (a no-asset DAG is byte-unchanged — no stray import). Blank-only lists treated as empty. **Frontend:** `ir.ts` (+`schedule_assets`, +`inlets`/`outlets`), `graph.ts` (round-trip, persisting only non-empty filtered lists), `forms.ts` (DAG-form **"schedule on assets"** comma-sep field like `tags`; node-form **`__assets__`** nested "Assets (data-aware scheduling)" fieldset with inlets/outlets comma-sep, ordered after Common settings; `nodeToFormData`/`formDataToNode` round-trip via `parseTags`), `NodeTab.tsx` (`NodeForm` threads inlets/outlets through `onNodeChange`). **Tests:** +3 `test_codegen.py` (DAG asset schedule + task outlets/inlets incl. native@task & operator; both families; no-asset⇒no-import + blank-only⇒empty), +1 `graph.spec.ts` (node inlets/outlets round-trip, blank-drop), +3 `forms.spec.ts` (node `__assets__` fieldset + round-trip; DAG `schedule_assets` round-trip; fixed 2 existing nodeForm tests for the always-present `__assets__` section). Verified: **152 server pytest** (was 149: +3), **84 jest** (was 80: +4), tsc/eslint/prettier/stylelint 0, dev build OK. PRD **new §6.9** + §5 v1.2 (✅) + §14 v1.2 row + §8.3 IR + §6.1.3 DAG/NODE field lists + §15.1 (DAG schedule-on-assets) + §15.3 (NODE Assets section) updated. **Deferred 🔭:** `AssetAlias`, `AssetAny`/`AssetAll` boolean schedules, watchers, `group`/`extra`, the `@asset` decorator, a combined `AssetOrTimeSchedule` (all addable with no IR change). Then ran a **3-dimension adversarial review** (wheel+codegen / frontend round-trip / design-semantics; reviewers + per-finding verifier against the task-sdk wheel). The first run hit 2 transient API-500 agent deaths (the `design` dimension + one verifier), so the **design dimension was re-run as a focused agent against the installed wheel at runtime** → **0 confirmed defects across all dimensions; 0 refuted (none survived to verify).** The design re-review confirmed at runtime: (1) **producer/consumer identity matches** — `outlets=[Asset(s)]` and `schedule=[Asset(s)]` build the identical `(name, uri)` / `AssetUniqueKey` for the same string (incl. plain names, URIs, trailing-slash, query-reorder), so the trigger loop fires; (2) **list = AND semantics** — `schedule=[Asset(a), Asset(b)]` coerces to `AssetAll` (DAG runs when ALL update), matching the PRD/help wording; (3) the **override is clean** (one `schedule=` kwarg, no duplicate; help text accurate); (4) **`@task` genuinely forwards `inlets`/`outlets`** to the underlying operator (not mis-bound as function params), both families, Asset import only-when-used, blank entries stripped. Applied the one suggested non-defect tweak: reworded the §6.9 intro "instead of (or as well as) a clock" → "instead of a clock (hard override; combined `AssetOrTimeSchedule` deferred)" to remove ambiguity. (The other minor note — codegen can't catch reserved asset names like `Asset('self')` that raise at Airflow parse time — is correctly out of scope: the PRD claims no asset-name validation and `/importErrors` + §7 friendly recovery is the deploy verdict.)

- [x] 2026-06-23 — **Advanced KubernetesPodOperator surface (v1.2; §6.2.1 KPO bullet / §5).** Filled in the deferred 🔭 advanced KPO params (user-picked v1.2 track). **Wheel-verified** the exact `KubernetesPodOperator.__init__` against `apache-airflow-providers-cncf-kubernetes==10.18.0` (zipfile+ast): the typed params are `volumes`/`volume_mounts: list[k8s.V1Volume/V1VolumeMount]`, `secrets: list[Secret]`, `affinity: k8s.V1Affinity`, `container_resources: k8s.V1ResourceRequirements`, `tolerations: list[k8s.V1Toleration]`, while `node_selector`/`labels`/`annotations` are plain `dict`, `security_context` accepts a `dict`, `service_account_name`/`priority_class_name`/`pod_template_file` are `str`, and `pod_template_dict` is a raw manifest `dict` (verified consumed via `PodGenerator.deserialize_model_dict` at pod.py:1474). **Design decision (data-only, no codegen change, no extra imports):** added **8 dedicated fields** — `node_selector`/`labels`/`annotations`/`security_context` (json), `service_account_name`/`priority_class_name`/`pod_template_file` (text), `pod_template_dict` (json) — and **routed the typed-object surface (volumes/secrets/affinity/tolerations/container_resources) through `pod_template_dict`/`pod_template_file`**, the Airflow-idiomatic declarative path (a JSON pod manifest Airflow deserializes + merges with the fields) that needs no nested `k8s.V1*`/`Secret` constructions or `kubernetes` imports — so codegen stays data-only with zero unused imports, and every advanced setting is still expressible from the editor. KPO has no `code` param, so the existing render-time blank-strip keeps the constructor clean; render-verified across 6 combos in both families (`ast.parse`+`compile`, no stray blanks). Description/help updated to map the typed surface → pod_template_dict (with a resources example). Tests: +1 `test_registry.py` (8 advanced params present, correct json/text widget, all optional, no code param introduced), +1 `test_codegen.py` (advanced fields render as dict/str literals; pod_template_dict literal; **no `k8s.`/`from kubernetes` import**; no stray blank). Also flipped two **stale wireframe markers** to ✅ that the Done log already covered but §15 hadn't reconciled (CLAUDE.md sync): **§15.4** CODE-tab syntax highlighting + line numbers (the CodePanel already uses `CodeMirrorField language="python" readOnly`) and **§15.11** DAG rename (parts A+B shipped 2026-06-16). Verified: **149 server pytest** (was 147: +2), **80 jest** (unchanged — no frontend file touched this round), tsc/eslint/prettier/stylelint 0, dev build OK. PRD §5 v1.2 + §6.2.1 KPO bullet (advanced surface 🔭→✅) + §14 v1.2 row + §15.4/§15.11 updated. Then ran a **2-dimension adversarial review** (wheel-fidelity+codegen / design-soundness+docs; 2 reviewers + per-finding verifier reading the cncf-kubernetes 10.18.0 wheel; 4 agents) → **0 confirmed findings, 2 refuted.** Both verifiers independently confirmed against the wheel: all 8 params are real `KubernetesPodOperator.__init__` kwargs with compatible types, `security_context` accepts a `dict`, `pod_template_dict` is consumed via `deserialize_model_dict` then `reconcile_pods(template, pod)` (so the help's "merged with the fields above" is accurate — operator settings win on top of the template base), codegen emits valid Python with no stray blanks and **no `k8s`/`kubernetes`/`Secret` import**. The 2 refuted items were wording-clarity nits on accurate help text (the task scoped to real defects only) — but both flagged a genuine real-world footgun, so I **applied them anyway** as one-line help improvements: `pod_template_file`/`pod_template_dict` are mutually exclusive (the file silently wins if both set), and `pod_template_dict` container settings merge **positionally** (name the first container `"base"` to match the operator's default). Re-verified after the edit: registry reload + render OK, KPO tests pass.

- [x] 2026-06-23 — **Third-party P3 lakehouse ops (§6.2.2) + the §13 Q13 gating extension** (catalogue 42 → 44). The last v1.3 operator tranche: the two **off-constraints** ops. **Wheel-verified** (zipfile+ast on the exact live wheels): `airflow-provider-great-expectations==1.0.0` and `openmetadata-ingestion==1.13.0.0`. **Two big wheel-check catches:** (1) GE's legacy **`GreatExpectationsOperator` no longer exists in v1.0.0** — it was replaced by three `GXValidate*Operator` classes that all take Python **callables** (`configure_checkpoint`/`configure_batch_definition`/`configure_dataframe`); the research-doc/PRD name was stale → modelled **`GXValidateCheckpointOperator`** (single `configure_checkpoint` callable). (2) `OpenMetadataLineageOperator(server_config: OpenMetadataConnection, service_name, only_keep_dag_lineage=False, max_status=10)` — `server_config` is a **constructed pydantic object**, not a string/dict. So **both ops are inherently code-first**: each YAML has a `code`-widget param holding a user callable body (mirroring `python_task`/`branch`); the template wraps it in a function and references it — GE passes the **function** (`configure_checkpoint=configure_checkpoint_<tid>`), OpenMetadata passes the **called** result (`server_config=server_config_<tid>()`). Optional kwargs use Jinja `{%- if -%}` whitespace trimming (the operator-block blank-strip is skipped for code-param ops, to preserve the user body) so an omitted optional leaves no stray blank — render-verified across 8 param combos in both families (`ast.parse`+`compile`). Imports: `great_expectations_provider.operators.validate_checkpoint` / `airflow_provider_openmetadata.lineage.operator` (NOT apache-namespaced). **Gating extension (§13 Q13, resolved option B):** new `third_party: true` registry flag + own `version`; a distinct **`third-party`** availability state — `availability(..., third_party=True)` → `third-party` always (independent of `/api/v2/providers`, which isn't an authoritative install signal for off-constraints pkgs — twilio-like SDKs don't register, and it can't confirm OpenMetadata's *server*-version match). `provider_block_errors` **skips** third-party ops (deploy never hard-blocked; `/importErrors` + §7 friendly recovery is the verdict); the version-pinned install hint (`pip install <pkg>==<v>`) rides `annotate_view`. Files: `registry.py` (`_CLIENT_DOC_FIELDS` ships `thirdParty`/`version`), `providers.py` (`availability` 3rd-party branch, `pip_install_hint(version=)`, `annotate_view`, gate skip); `interfaces.ts` (+`thirdParty`/`version`/`third-party` on `IOperatorDef`+`INotifierDef`), `Palette.tsx` (un-dimmed `ⓘ` info glyph + pinned-install title), `InfoTab.tsx` (third-party note), `afdag.css` (`-item-info`/`-info-thirdparty`). Two new YAMLs: `great_expectations.yaml`, `openmetadata_lineage.yaml` (both families, `third_party:true`, version pin, descriptions steer simple users to native SQL checks + carry the OpenMetadata server-match caveat). Tests: +1 `test_registry.py` (P3 ids/providers/imports/third_party/version/code-param + client_view ships thirdParty/version, withholds import/template), +3 `test_codegen.py` (render both families incl. function-wrap/call-vs-pass + boolean/int literals + imports; omitted-middle-optional no-stray-blank + user-body blanks preserved; optional-defaulted params guarded), +3 `test_providers.py` (availability third-party never-blocks + version pin; annotate pins version not missing-provider; gate skips third-party while still blocking a normal missing provider). Verified: **147 server pytest** (was 140: +7), **80 jest** (8 suites; via the Node-18 `File` polyfill preload — CI on Node 20 unaffected), tsc/eslint/prettier/stylelint 0, dev build OK. PRD §5/§6.2.2 (P3 rows → ✅, intro, ¹ footnote, P3-shipped paragraph)/§13 Q13 (resolved)/§14 v1.3/§15.1/§15.7 (third-party palette state) updated. **Remaining (shared with P0–P2, env-gated):** the live deploy on the devcontainer `3.0.2` — Airflow + the third-party packages aren't installed in this Jupyter env (wheel-inspection + codegen `compile` stand in). Then ran a **3-dimension adversarial review** (wheel-fidelity+codegen / gating logic / frontend; 3 reviewers + per-finding verifier reading the real wheels; 6 agents) → **2 findings, both confirmed (both LOW), both fixed; 1 refuted.** (LOW, GE) `context_type` (an optional with a registry `default`) was emitted **unconditionally** → a blank/absent field rendered `context_type=None` (or `=''`) instead of letting the operator's own `'ephemeral'` default apply — and since `forms.ts` never seeds a registry `default` into the IR, that was the **default** path for a freshly-dropped node, not a rare edge → **guarded it** like the sibling optionals (omit → operator default). (LOW, OpenMetadata) `max_status` used a truthiness guard `{% if params.max_status %}`, so an explicit **`max_status=0`** was silently dropped to the wheel default 10 → switched to `{%- if params.get('max_status') is not none %}` (the verifier flagged that the naive `params.max_status is not none` would emit `<Undefined>` when absent — used `params.get(...)`). Both re-rendered clean (no stray blanks, valid Python) + pinned by a new regression test. (1 **refuted:** the Palette `thirdPartyHint` pip fallback omitted the version pin that the INFO-tab fallback includes — confirmed unreachable dead code, since the server always sets a version-pinned `pipInstall` for a third-party op; tidied anyway for symmetry.) Re-verified after fixes: 147 server pytest, 80 jest, tsc/eslint/prettier/stylelint 0, dev build OK.

- [x] 2026-06-22 — **Per-task callbacks (§6.8) — `node.callbacks` + the task-only `on_retry`.** The per-task analogue of DAG-level callbacks: attach notifiers to a single task's `on_failure`/`on_retry`/`on_success` events. Grounded by a 3-agent **understanding workflow** that (a) audited all 43 operator templates — **every one has a `{{ common | pyargs }}` trailing-kwargs slot in both families** (zero silent drops) — and (b) **wheel-verified** (apache-airflow-task-sdk 1.2.2, zipfile+ast) that all three task-level callbacks **fire** in Airflow 3 and the `@task` decorator forwards them to the underlying operator (there is no task-level `sla_miss`; `on_execute`/`on_skipped` fire but are niche → deferred). **Codegen** (`codegen.py`): `_TASK_CALLBACK_EVENTS = (on_success, on_failure, on_retry)`, shared `_render_callback_entries` (factored out of `_build_callbacks`), new `_node_callbacks` → per-task `on_*_callback=[…]` exprs **merged into the node's `common` dict** (`common={**_node_common, **node_cb_kwargs}`) so every operator template emits them with **no per-operator edit** — into the `@task(…)` decorator for native ops, the operator call otherwise, in **both** TaskFlow & Traditional; per-task notifier imports collected; unknown notifier → `CodegenError`. **Deploy gate** (`providers.py`): `provider_block_errors` factored a `_scan_callbacks` helper applied to `dag.callbacks` **and** every `node.callbacks`. **Frontend:** IR `IAfdagTaskCallbacks` (+`on_retry`) + `AfdagCallbacksValue` on `IAfdagNode.callbacks`; `graph.ts` round-trips it with a `pruneCallbacks` (empty event arrays dropped, back-compatible). **Refactored** the DAG-level NOTIFY tab into a shared **`CallbacksEditor`** (uid-keyed local state, json round-trip, availability dimming, inline required errors) — `NotificationsTab` is now a thin wrapper, and a new **"Notifications" section in the NODE tab** (`NodeCallbacksSection`) reuses the same editor for per-task callbacks; `StudioApp.errorCount` validates per-task notifier params too (gated on `notifiersReady`). Themed CSS. **Tests:** +5 `test_codegen.py` (taskflow decorator render incl. `on_retry`; operator-call render + common/callback coexist order in both families; native-op-in-Traditional; **event-order pinned to the fixed tuple**; unknown node notifier), +1 `test_providers.py` (gate scans node callbacks), +1 `graph.spec.ts` (round-trip + empty-array prune). Verified: **140 server pytest** (was 134), **80 jest** (8 suites), tsc/eslint/prettier/stylelint 0, dev build OK. PRD §5/§6.1.3/§6.8/§8.3/§13-Q15/§14/**§15.1**(NODE-tab Notifications section)/**§15.14** flipped to ✅ (DAG-level **+ per-task**). Then ran a **3-dimension adversarial review** (codegen+wheel / frontend / integration; 3 reviewers + per-finding verifier, 6 agents) → **3 findings, all confirmed but all LOW, all addressed:** (LOW, frontend) the NODE tab showed **stale state on an external `.afdag` reload** while the same node stayed selected — a **pre-existing** `NodeForm` property (not a regression), fixed in one stroke by **threading `reloadKey` into `NodeTab`** and folding it into both child keys (`${reloadKey}:${node.id}` / `${reloadKey}:cb:${node.id}`), matching DAG/NOTIFY reseed semantics (verified `reloadKey` bumps only on external reloads, so no focus-loss); (LOW, test gap) **event order was never asserted** (a refactor to `items()`-order iteration would pass CI) → added `test_node_callbacks_event_order_follows_fixed_tuple` (scrambled IR order, asserts success→failure→retry); (LOW, test gap) **native-op-in-Traditional callbacks** weren't directly tested → added `test_node_callbacks_render_on_native_op_traditional`. The codegen and integration reviewers found the merge approach, the gate dedup, and the round-trip correct. Re-verified after fixes: 140 server pytest, 80 jest, tsc/eslint/prettier 0. **Deferred 📝:** the niche `on_execute`/`on_skipped` task events (addable with no IR change).

- [x] 2026-06-22 — **More notifier channels (§6.8 / §6.2.2 P3) — 2 → 5 channels.** Added 3 wheel-verified notifier registry YAMLs to `jupyterlab_airflow/notifiers/`: `apprise` (`AppriseNotifier`, params body*/title/tag[default `all`]/apprise_conn_id[default `apprise_default`]), `discord` (`DiscordNotifier`, params text*/discord_conn_id[default `discord_webhook_default`]/username), `opsgenie` (`OpsgenieNotifier`, params payload*[a JSON **object**, `widget: json`, carrying a required `message`]/opsgenie_conn_id[default `opsgenie_default`]). Verified import module + class + every emitted kwarg + defaults against the EXACT live provider wheels (apprise 2.3.4, discord 3.12.3, opsgenie 5.10.4) via `pip download --no-deps` + `zipfile`/`ast`. **Wheel-check catches:** (1) the Discord **notifier** uses `discord_conn_id` (NOT `http_conn_id` like the Discord *operator*); (2) `OpsgenieNotifier` takes a single `payload` **dict** (not individual fields) → codegen emits a dict literal `OpsgenieNotifier(payload={'message': …, 'priority': …})`; (3) Apprise `notify_type` deliberately NOT exposed (it's a `NotifyType` enum — body/title/tag/conn is a valid working subset); (4) **no `TelegramNotifier`** — `airflow/providers/telegram/notifications/` doesn't exist in telegram 4.9.5, so Telegram is correctly omitted (operator-only). Each ships a single one-line Jinja `template` (notifiers have no Traditional/TaskFlow split) with `{% if %}`-guarded optionals; they flow through the existing `dag.callbacks` machinery (NOTIFY tab, `_build_callbacks`, `_collect_imports`, the `provider_block_errors` deploy gate) with **zero app-code change** — registry-driven. Tests: extended `test_registry.py` (notifier test now asserts all 5 ids/providers/import-modules + no `airflow.sensors.` leak) and added `test_dag_callbacks_render_apprise_discord_opsgenie` to `test_codegen.py` (renders an on_failure list with all 3 incl. the Opsgenie dict literal + the 3 import lines). Verified: **134 server pytest** (was 133); no frontend change (YAML + tests only). PRD §5 v1.3 / §6.2.2 P3 (→✅, 5 channels) / §6.8 (registry bullet + closing) / §15.14 updated. Then ran a **focused adversarial review** (1 reviewer + per-finding verifier reading the wheels) → **0 findings** (the reviewer confirmed every kwarg/default/import against the wheels, the Discord `discord_conn_id`-not-`http_conn_id` distinction, the Opsgenie dict payload, and the absent TelegramNotifier).

- [x] 2026-06-22 — **Notifications & callbacks — DAG-level (§6.8 / §15.14).** A new full-stack feature: alert a channel (email/Slack) on a DAG lifecycle event via Airflow **callbacks** (not graph tasks). **Server:** a **notifier registry** mirroring the operator one — `jupyterlab_airflow/notifiers/*.yaml` + `load_notifiers`/`notifier_client_view` (refactored the shared parse into `_parse_files`; `AIRFLOW_NOTIFIERS_DIR` override) with 2 wheel-verified starters `smtp` (`SmtpNotifier`) + `slack` (`SlackNotifier`, base `BaseNotifier`); each is `import` + a one-line Jinja `template` rendering the notifier *instance* + params/help/provider. Codegen `_build_callbacks` reads `ir.dag.callbacks` (events `on_success`/`on_failure` — `sla_miss` deliberately excluded after wheel-checking the task-SDK `DAG`: **SLAs were removed in Airflow 3.0**, so `sla_miss_callback` only warns and never fires), renders each `{notifier_id, params}` via its template, and appends `on_<event>_callback=[…]` through `_dag_args` (so it works in **both** families — `@dag(…)` and `with DAG(…)`); notifier imports flow through `_collect_imports`. Unknown notifier → `CodegenError`. Endpoint `GET notifiers` (`NotifiersHandler` + `annotated_notifiers`, provider-gated like operators). **Frontend:** IR `IAfdagCallbacks`/`IAfdagCallbackEntry` + `dag.callbacks?` (carried into the IR by `flowToIR` for free); `INotifierDef`; `listNotifiers` + a `notifiers.ts` module cache (mirrors `operators.ts`); `notifierForm` (reuses `paramSchema`/`paramUi`); a new **`NotificationsTab`** = the 6th inspector tab (DAG·NODE·INFO·**NOTIFY**·CODE·SAVED) — per event, add/remove notifiers each with a registry-driven RJSF form (the `help`/`ⓘ`-bubble machinery), an unavailable-provider note, clean empty-param/empty-callback omission; `StudioApp` loads the notifier cache at activation (a ready flag re-renders). Themed CSS. **Tests:** +3 `test_codegen.py` (callbacks render in both families + imports; unknown-notifier rejected; no-callbacks-unchanged), +1 `test_registry.py` (notifiers load + client_view withholds import/template), +1 `forms.spec.ts` (`notifierForm`). Verified: **132 server pytest** (was 128), **79 jest** (8 suites), tsc/eslint/prettier/stylelint 0, dev build OK. PRD §5/§6.1.3/§6.8/§8.3/§13-Q15/§14/§15.14 flipped to ✅ (DAG-level). **Deferred 📝:** per-task `node.callbacks` + the `on_retry` event, more notifiers (Apprise→Teams/WhatsApp, Discord/Telegram/Opsgenie). **Pre-review wheel-check catch:** dropped `sla_miss` after inspecting the task-SDK `DAG` — SLAs were removed in Airflow 3.0 (the kwarg only warns, never fires). Then ran a **3-dimension adversarial review** (3 reviewers + per-finding verifier; 11 agents) → **8 findings, 6 confirmed (2 med, 4 low; 2 refuted), all fixed:** (MED) the deploy provider hard-gate (`provider_block_errors`) ignored notifier providers → now scans `dag.callbacks` too (+test, server pytest 133); (MED) required notifier params weren't validated → new `validateNotifierParams`, folded into `StudioApp.errorCount` (Deploy blocks) + an inline "Required: …" note per entry; (LOW) json/object notifier params didn't round-trip → `notifierToFormData`/`formDataToNotifierParams` + per-entry local form state (no mid-edit reformat); (LOW) version-too-old note now distinct from missing-provider; (LOW) the Add buttons dim unavailable channels (palette parity); (LOW) entries keyed by a stable client uid (not array index) so mid-list removal reconciles correctly. (2 refuted: the Slack `text` / SMTP `from_email` required flags are correct as-is.) Re-verified: 133 server pytest, 79 jest, tsc/eslint/prettier/stylelint 0, dev build OK.

- [x] 2026-06-22 — **P2 lakehouse operators (§6.2.2), catalogue 33 → 42.** 9 registry YAMLs (both template families), driven by a parallel **spec-verification workflow** (one verifier per provider reading the real wheels: ftp 3.15, imap 3.11, amazon 9.30, apache-spark 6.1, cncf-kubernetes 10.18, discord 3.12, telegram 4.9, opsgenie 5.10): `ftp_file_transmit` + `ftp_sensor` (ftp), `imap_attachment_to_s3` (**amazon** transfers) + `imap_attachment_sensor` (imap), `spark_jdbc` (apache-spark) + `spark_kubernetes` (cncf-kubernetes), `discord_webhook` (discord) + `telegram` (telegram) + `opsgenie_create_alert` (opsgenie). Boolean kwargs declared `type: boolean` so codegen emits Python `True`/`False`; dict/list kwargs (`template_spec`/`tags`/`details`) use `type: object`/`array` + `widget: json`. **Wheel-check catches baked in:** `ImapAttachmentToS3Operator` is in **amazon** not imap (like `SFTPToS3`); the IMAP **sensor**'s conn kwarg is `conn_id` (not `imap_conn_id`); Telegram's real message kwarg is **`text`** (not `message`); Discord's required conn kwarg is **`http_conn_id`**; `SparkKubernetes` takes one of `application_file`/`template_spec`. All 9 render valid Airflow-3 Python in both families (ast.parse+compile), no stray blanks, no method leaks; the sensors carry the sensor common_params. Then ran a **focused adversarial review** (2 reviewers + per-finding verifier) → **2 findings, both confirmed (1 med, 1 low), 0 refuted — both on `spark_kubernetes` help/description**: (MED) "with neither, the DAG won't load" was wrong — the one-of is a **runtime** `AirflowException` (the DAG parses fine; `__init__` doesn't validate, `manage_template_specs()` raises at execute) → reworded to "the task fails at run time"; (LOW) "EXACTLY ONE" overstated — providing both doesn't raise, `application_file` silently wins → reworded to "at least one (the file wins if both)". The other 8 ops had no findings. Tests: +1 `test_registry.py`, +1 `test_codegen.py` (render all 9 + import lines + boolean/json emission). Verified: **128 server pytest** (was 126); no app code changed (YAML + tests only). PRD §5/§6.2.2/§14/§15.1 flipped P0–P2 → ✅ (catalogue → 42); only third-party **P3** (GE/OpenMetadata) + the §6.8 notifier callbacks remain. **Remaining:** the live deploy on the devcontainer `3.0.2` (env-gated; queued in Pending).

- [x] 2026-06-22 — **P1 lakehouse operators (§6.2.2), catalogue 26 → 33.** 7 registry YAMLs (both template families), wheel-verified against amazon 9.30 / sftp 5.8 / apache-spark 6.1 / slack 9.10: `s3_copy_object`, `s3_list`, `s3_delete_objects` (Storage, amazon), `sftp_sensor` (Sensors, sftp — full sensor `common_params`), `sftp_to_s3` (Ingestion, **amazon**), `spark_sql` (Compute), `slack_webhook` (Notifications). Verified every import path/class/kwarg against the real wheels (`pip download --no-deps` + `zipfile`/`ast`); each renders valid Airflow-3 Python in **both** families (`ast.parse`+`compile`), optionals `{% if %}`-guarded, `common` emitted, no Airflow-2 paths, `client_view` withholds server-only fields. **Two catches the wheel-check surfaced:** (1) **`SFTPToS3Operator` lives in the `amazon` provider's `aws/transfers`, not `sftp`** — the research doc's listing was wrong; corrected the YAML import + the §6.2.2 table. (2) the `S3DeleteObjectsOperator` `keys` param **collides with the dict `.keys` method in Jinja** (`params.keys` → bound method, emitting `<built-in method keys…>`), so the template uses **`params['keys']`** — caught by rendering before the test, with a regression assertion added. `s3_delete_objects` carries the destructive blast-radius warning. Then ran a **focused adversarial review** (2 reviewers + per-finding verifier) → **3 findings, 2 confirmed, 1 refuted; both fixed:** (HIGH) the `params['keys']` guard was **still wrong** — Jinja's `params['keys']` falls back to the dict `.keys` METHOD when the key is **absent**, so a legitimate **prefix-only** delete emitted `keys=<built-in method…>` (invalid Python) → switched to **`params.get('keys')`** (explicit call → `None` when absent); +regression test for the keys **and** prefix-only paths. The op needs Keys **or** Prefix (description now says so; neither → a clear Airflow import error surfaced by §7). (MED) `slack_webhook` `channel` help wrongly implied an override worked — **standard Slack Incoming Webhooks ignore `channel`** (only legacy honour it; verified in the slack 9.10 hook's `LEGACY_INTEGRATION_PARAMS` + `UserWarning`) → reworded. (1 refuted: the "empty-keys silent no-op" claim — the guard omits empty keys → a loud import error, not a silent no-op.) Tests: +1 `test_registry.py` (7 ids/providers/import-modules/categories/operator+both-templates; sftp_sensor sensor common_params), +2 `test_codegen.py` (render all 7 + import lines; the keys-vs-prefix `.get` regression). Verified: **125 server pytest** (was 123), tsc/eslint clean on the (unchanged) frontend; no app code changed (YAML + tests only). PRD §5/§6.2.2/§14/§15.1 flipped P1 → ✅ (catalogue → 33). **Remaining:** the live deploy on the devcontainer `3.0.2` (env-gated; queued in Pending). P2 + Notifications/callbacks remain specced.

- [x] 2026-06-22 — **Adversarial review of the 3 v1.3 tracks + fixed all findings.** Ran a 3-dimension review workflow (3 reviewers — bubbles / log viewer / operators — + 1 adversarial verifier per finding; 14 agents) → **11 findings, all 11 confirmed (1 high · 4 med · 6 low), all fixed; 0 refuted** (each was reproduced against the real code/wheels, several with executable probes). **InfoBubble:** (med) the click-toggle raced `onFocus` so the first tap opened-then-closed the bubble → rewrote the open state to separate `hovered`/`focused`/`dismissed` flags (`open = (hovered||focused) && !dismissed`) and dropped the click toggle (focus covers tap); (low) removed the misused `aria-expanded` from the tooltip trigger (kept `role=tooltip` + `aria-describedby`); (low) a stray mouse-leave/blur no longer closes a bubble the other source still holds open. **forms.ts:** (low) the `start_date` help contradicted the shipped `catchup=False` default → reworded to decouple from catchup. **LogViewer:** (HIGH) `classifyLine` matched a level token *anywhere* in the line, so a benign INFO line mentioning "ERROR" was painted red AND stole the autoscroll-to-first-error → anchored the match to Airflow's `{file:lineno} LEVEL` position (leading-token + traceback fallback); +1 spec locking the false positives (16/16 cases); (low) `Copy` was a silent no-op in a non-secure (HTTP) context → feature-detect + `textarea`/`execCommand` fallback + a transient Copied/Copy-failed label. **ManagerApp `Overlay`:** (med) the global Escape listener hijacked the native "clear search" gesture and tore down the modal → bail when focus is in an INPUT/TEXTAREA/SELECT/contentEditable or the event was already handled; (med) focus-on-open targeted the `display:contents` inner wrapper (a no-op) → focus the real `.jp-airflow-modal` box (tabindex=-1, no `:focus-visible` ring); (low) the stale-log-response guard omitted `dagId` → added it. **Operators:** (med) `slack_post` `channel` help wrongly claimed a connection default → corrected to the real `#general` fallback (verified in the slack 9.10 wheel: `channel="#general"` hardcoded); (low) `email` `to` help implied a single recipient → noted comma/semicolon-separated (the smtp hook splits on `,`/`;`). Verified: 123 server pytest, 78 jest (8 suites, +1 new LogViewer case), tsc/eslint/prettier/stylelint 0, dev build OK.

- [x] 2026-06-22 — **Implement 3 of the v1.3 tracks** (user picked field bubbles + log viewer + P0 operators, and chose to **keep Traditional**). **(1) Field info bubbles (§6.1.3/§15.1/§15.3):** new `src/components/InfoBubble.tsx` — an accessible `ⓘ` glyph + tooltip (`role="tooltip"`, `aria-describedby`, opens on hover/focus/click, dismiss on `Esc`/blur); wired **once** as a custom RJSF `DescriptionFieldTemplate` in `AfdagForm.tsx` (`templates` prop), so every field's schema `description` renders as a bubble across BOTH the registry-driven NODE form and the hand-written DAG form. Filled a `description` on all 9 bare DAG fields in `forms.ts` `dagForm()` (reusing the INFO-tab `DAG_CONCEPTS` wording). Themed CSS (`.jp-afdag-info-bubble/-glyph/-tooltip`) in `afdag.css`. Test: +1 `forms.spec.ts` (every DAG field carries help). **(2) Friendly log viewer (§6.6/§15.9):** new `src/components/LogViewer.tsx` replaces the raw `<pre>` — per-line level classification **client-side from the line text** (INFO/WARN/ERROR/CRITICAL/DEBUG; a Python traceback with no token → error), per-level colour + an error left-bar (non-color-only), **autoscroll-to-first-error**, an **attempt selector** (try 1…N, re-fetched via the existing API), search, errors-only filter, Copy/Download/Wrap, and a loading/error state distinct from content; `ManagerApp.loadLogs` ignores a stale response on a task/try switch; `Overlay` gained **Escape-close + focus-on-open** (all dialogs benefit). **No server change** — line-text classification avoids the structured-event contract churn (noted as a future enhancement). New `LogViewer.spec.ts` (`classifyLine`: tokens, traceback-as-error, severity precedence, plain fallback). CSS in `base.css`. **(3) P0 lakehouse operators (§6.2.2), catalogue 18→26:** 8 registry YAMLs — `s3_create_object` (Storage, amazon), `sftp_transfer` (Ingestion, sftp), `sql_column_check`/`sql_table_check` (Data Quality, common-sql), `spark_submit`/`papermill` (Compute), `email` (Notifications, smtp), `slack_post` (Notifications, slack); each `taskflow: operator` with BOTH templates + `help`/`description`/`docs_url`/`example` + `common_params`. **Import paths, classes, and every emitted kwarg verified against the real provider wheels** (amazon 9.30, sftp 5.8, common-sql 2.0, apache-spark 6.1, papermill 3.13, smtp 3.0, slack 9.10) via `pip download --no-deps` + `zipfile`/`ast` inspection — caught that `SFTPOperator` uses `ssh_conn_id` (not `sftp_conn_id`) + `operation="put"`, and the Slack default conn is `slack_api_default`. Codegen renders valid Airflow-3 Python in BOTH families (`ast.parse`+`compile`), optionals `{% if %}`-guarded (required-first, blank-stripped), `common` emitted; no Airflow-2 paths; `client_view` withholds server-only fields. Tests: +1 `test_registry.py` (8 ids/providers/import-modules/categories/operator+both-templates), +2 `test_codegen.py` (render all 8 + import lines; optional-omission leaves no blank). **Verified:** 123 server pytest (was 120), 77 jest (8 suites, all green), `tsc`/`eslint`/`prettier`/`stylelint` 0 on every touched file (`build:lib` clean). Added a guarded Node-18 `File` polyfill preload for the jest run (jsdom env needs it; CI on Node 20 unaffected). PRD §5/§6.1.3/§6.2.2/§6.6/§7/§14/§15.1/§15.3/§15.9 flipped to ✅. **Not done (env-gated, queued in Pending):** a **live deploy on the devcontainer `3.0.2`** — Airflow + the providers aren't installed in this Jupyter env, so wheel-inspection + codegen `compile` stand in for it. No adversarial-review pass this turn.

- [x] 2026-06-22 — **Spec + plan four new feature tracks in the PRD** (user request: redesign the DAG-log dialog; add field info bubbles on DAG + node config; remove the Traditional button — TaskFlow-only; add & plan the `scope/airflow3-lakehouse-operators.md` operators). Grounded by a 5-agent understanding workflow (mapped every Traditional touchpoint with file:line; the log-dialog chain `ManagerApp`→`client.get_task_logs`; the field-help mechanism — node params already render inline RJSF `help`, **DAG fields are bare** + no tooltip primitive exists; the operator-registry authoring contract; and cross-referenced the operators doc against the 18 existing YAMLs → gap list + notifier-vs-operator split). PRD edits: **(1) Remove Traditional → TaskFlow-only** — *specced across §5/§6.1.4/§6.2/§6.3/§6.7/§10/§11/§13/§14/§15 + Appendices A/C, then **fully reverted** when the user chose to **keep Traditional**; the PRD is back to "both backends shipped ✅" and no code was touched.* **(2) §6.2.2 Lakehouse operator expansion** — a P0→P3 roadmap table (Storage/Ingestion/Compute/DataQuality/Notifications/Governance) + the Airflow-3 ground rules (per-DB SQL deprecated→`SQLExecuteQueryOperator`+conn; MinIO=S3 via `endpoint_url`; NiFi via `HttpOperator`; JupyterLab via Papermill; Email→smtp; Dask gone; `common-io` XCom-backend / OpenLineage listener / managed-apis plugin are **not** palette nodes; operator-vs-notifier disambiguation; third-party off-constraints caveat). **(3) §6.6 + §15.9 friendly log viewer** (level colour, attempt selector, search, Copy/Download/Wrap, autoscroll-to-error; server stops flattening structured events). **(4) §6.1.3 + §15.1 + §15.3 field ⓘ info bubbles** (inline help on every DAG field + a hoverable accessible bubble wired once in `AfdagForm`). Plus **§6.8 + §15.14 Notifications & callbacks** (new IR `callbacks` block + notifier registry + tab) to host deferred notifiers; **§5 v1.3** scope tier; **§14 v1.3** milestone; **§10** test notes; **§8.3** callbacks-in-IR note; **§13 Q13–16**. Implementation queued in Pending (order: field bubbles → log viewer → operators P0→P2 → notifications; **Traditional stays**). **No app code changed this turn** — spec + plan only.

- [x] 2026-06-21 — **Friendly import-error recovery** (M6, §7) — the make-or-break recovery surface, the last unbuilt MVP milestone. Closed the gap where a failed import dumped a raw traceback in both the editor deploy banner and the manager import-errors panel with no translation, no node mapping, and no way back to the source. New pure, unit-tested **`src/importErrors.ts`**: `explainImportError(stack_trace, ir?)` → `{title, summary, hint?, nodeTaskId?}`, classifying the **final exception line** into missing-provider (with a `pip install apache-airflow-providers-…` line derived from the failed module via `providerPackageForModule`, splitting `airflow.providers.<x>.<y>.operators…` at the operators/sensors/hooks/transfers/triggers/utils boundary) · non-provider missing module · Airflow-2 import path · `cannot import name` · syntax/indentation · undefined-name · else the raw line; `matchNode` whole-word-matches a `task_id` in the trace (the generated Python names every task by `task_id`) for a best-effort "⚠ Check the **<task>** task" pointer. **Editor:** `StudioApp` memoizes `deployExplanation` from `deploy.importError.stack_trace` + the live IR and passes it to `DeployBanner`, which renders a themed card (`--jp-*`, error-bar) above the now-renamed *Show technical details* expander. **Manager:** server `find_source_path(filename|dag_id, contents_root)` (deploy.py) resolves a deployed `.py` → its source `.afdag` Contents path via the `afdag_id` provenance ↔ Contents-root join (refactored `_live_afdag_ids` onto a new `_afdag_paths` id→relpath walk; first-wins on dupes), exposed as `dags/source` (`DagSourceHandler`) → `findDagSource` handler → an **Open in Studio to fix** button per error in `ManagerApp` that opens the `.afdag` in the Studio factory (`openPath` threaded index→`AirflowPanel`→`ManagerApp`); each error now shows the friendly card (title/summary/hint) with the raw trace under *Show technical details*, and a clear message when the source is gone/pre-provenance. Tests: +13 jest (`importErrors.spec.ts` — provider-pkg mapping, node match incl. partial-id rejection, every classifier branch), +4 server (`find_source_path` by filename/dag_id incl. nested, None on deleted source / pre-provenance), +1 handler (`dags/source` wiring). Verified: 120 server, 72 jest (Node-18 `File` polyfill), tsc/eslint/stylelint 0, prettier clean, dev build OK. PRD §7 + M6 row flipped to ✅; §15.6 (banner ③b card) + §15.8 (manager import-error card + Open in Studio) wireframes/Built notes updated. (Advisory: node mapping is best-effort whole-word `task_id`; not a codegen source map.)

- [x] 2026-06-20 — One-click **Tidy layout** (dagre auto-layout, v1.1, §8.2) — the last remaining v1.1 roadmap item. Added `@dagrejs/dagre@3.0.0` dep (bundled, not a React singleton). New `src/layout.ts` `tidyLayout(nodes, edges)`: pure, builds a dagre `rankdir:TB` graph from the **task** nodes only (notes excluded via `isNoteNode`) using each node's measured size, lays out, and returns id→top-left positions (converting dagre's centre coords; rounded to match `flowToIR`). Top-bar `≣ Tidy` button in `StudioApp` (disabled when empty) applies positions via `setNodes`; the existing persist effect saves them (marks doc dirty) and the view re-fits. New `src/__tests__/layout.spec.ts` (3 specs: TB ordering, note exclusion, empty no-op) + `jest.setup.js` polyfilling `structuredClone` (jsdom lacks it; dagre needs it; via `v8` serialize, guarded) wired through `jest.config.js` `setupFiles`. PRD §5/§6.1.4/§8.2/§14/§15.1 (+caption) flipped 🔭→✅. Verified: tsc/eslint/stylelint 0, prettier clean, 60 jest (3 new), dev build OK + dagre bundled. (`build:prod` fails in `license-webpack-plugin` — confirmed **pre-existing at HEAD**, unrelated.) Ran a 3-dimension adversarial review (8 agents, per-finding adversarial verify) → **5 findings, 2 confirmed (both low), both fixed**: (1) the deferred `setTimeout(0)` fitView was the only one in the component never cancelled on unmount (`rfRef` is never nulled) → capture the handle in `tidyTimerRef` + clear it on unmount (mirrors the panel-collapse/resize effects); (2) the post-Tidy `fitView` had no `nodes` scope so it fit **all** visible nodes incl. note cards — a far-parked note would zoom out and shrink the tidied graph → scoped `fitView({ nodes })` to just the laid-out task ids. (3 findings were explicit "no defect" confirmations: tidyLayout math, the `structuredClone` polyfill faithfulness, and the glyph/a11y/unsaved-on-Tidy behaviour.) Re-verified after fixes: tsc/eslint/stylelint 0, 60 jest, dev build OK.
- [x] 2026-06-20 — First-run **onboarding** coachmark (§7) — closes the last MVP recovery/UX gap. New `Coachmark.tsx`: a dismissible, non-blocking 3-step card pinned top-centre of the canvas (add task → configure → deploy), with a step indicator (● ○ ○), *Skip tour*, and *Next →*/*Done*. `StudioApp` drives it: `coachStep` (0 hidden, 1/2/3) seeded from a `localStorage` `jp-afdag-onboarded` flag (shown once per browser; try/catch so a blocked store fails safe), advanced by effects (step 1→2 when the first task lands; finished the moment a Deploy starts from any step) + the manual Next/Done; `completeOnboarding` sets the flag and hides it. Themed CSS (`--jp-*`). UI-only — no server change. Verified: tsc/eslint/stylelint 0, prettier clean, 57 jest (no regression), dev build OK. PRD §7 + §15.2 (wireframe + note) flipped 🔭→✅. Ran a 2-dimension adversarial review (8 agents) → **5 confirmed (3 distinct, all low/med), all fixed**: (1) a not-onboarded user opening an *already-populated* `.afdag` mis-staged to step 2 → now `load()` starts the tour (step 1) only for a genuinely empty new doc (and only if not onboarded); (2, a11y) `role="dialog"` on a passive non-modal card set a focus-managed expectation it never honored + step changes weren't announced → switched to `role="status" aria-live="polite"` (a polite live region, matching DeployBanner); (3) the card trapped canvas clicks/drag over its footprint → `pointer-events:none` on the card with `auto` on the buttons (mirrors `.jp-afdag-empty`). (1 refuted: cross-editor localStorage sync on first run — narrow edge case, and the proposed `storage`-event fix wouldn't fire same-tab.) Re-verified: tsc/eslint/stylelint 0, prettier clean, 57 jest, dev build OK. (Tidy layout/dagre split out as the lone remaining v1.1 item — needs a new dep.)

- [x] 2026-06-20 — **Traditional codegen backend** (v1.1, §6.3) — unlocks the second syntax family (the editor was TaskFlow-only). Codegen `_render` now keys on `ir.syntax_style`: **TaskFlow** = `@dag`/`@task` + `task_id_task = task_id()` for native ops (unchanged); **Traditional** = `with DAG(...) as dag:` + every op as an operator instance (via the per-op `template_traditional`, already present) + `>>` wiring, no decorator/call. Extracted `_dag_args` (shared by `@dag` + `with DAG(...)`); `_dag_context`; `_collect_imports(ops, traditional)` switches to `from airflow.sdk import DAG` + operator-class imports; header `syntax={syntax}`. Replaced the blank-strip predicate `is_operator` → `not _has_code_param(op)` (an op with a `code`-widget param keeps its user body) — equivalent for TaskFlow (only bash changes, and it has no blanks there) and correct for Traditional (native bash/empty render as pure `Cls(...)` and get stripped, while branch/python/shortcircuit keep their `def _x()` body). Frontend: a segmented top-bar **TaskFlow / Traditional toggle** (`onToggleSyntax` updates `baseRef.syntax_style`, bumps `syntaxStyle` state so `currentIR` re-memos, and `commit()`s to persist); `syntaxStyle` seeded from the loaded IR; the CODE-tab header shows the active family; themed CSS. **Verified `from airflow.sdk import DAG` is exported by the Airflow-3 task SDK** (wheel inspection); a Traditional DAG generates valid Python (ast.parse+compile) and deploys (write succeeds; the dev dag-processor was too slow to confirm registration live, but the imports are all wheel-verified + standard). Tests: +3 codegen (traditional render incl. native-as-operator + code-node body + `>>`; default-is-taskflow; traditional common params). Verified: 114 server, 57 jest, tsc/eslint/stylelint 0, prettier clean, dev build OK. PRD §6.1.4 + §6.3 + §15.1 + §15.4 flipped to ✅. Ran a 3-dimension adversarial review (5 agents) → **2 confirmed (doc/test), both addressed**: flipping §6.3 to ✅ left stale "MVP emits TaskFlow only" / "ships only with golden-file equivalence tests" claims (§6.3/§10/§11/R7) AND the PRD's named R7 mitigation — a TaskFlow↔Traditional **equivalence test** — wasn't delivered (the new tests were one-sided/structural). Fixed properly: **added `test_taskflow_and_traditional_yield_the_same_task_graph`** (renders the same IR in both families, extracts the task set + `>>` edges via ast resolving handles→task_ids, asserts equal) — the actual R7 mitigation — and reconciled §6.3/§10/§11/§5-v1.1/R7/§14-v1.1 to "shipped ✅", with the code-node **context caveat** noted (TaskFlow `@task`/`get_current_context()` vs Traditional `PythonOperator(**context)`). 115 server tests.

- [x] 2026-06-20 — **Undeploy / rollback** (§7) — the make-or-break recovery surface. Server (`deploy.py`): every overwrite-deploy now backs up the prior managed `.py` to `{dag_id}.py.bak` (refactored the atomic write into `_atomic_write`; `write` copies the existing managed file to the backup before replacing — it already refuses non-managed files, so a pre-existing file is always Studio's); `{dag_id}.py.bak` doesn't end in `.py` so the dag-processor never parses it (+ `*.bak` added to `.airflowignore` defensively). New `restore_backup`/`has_backup`/`_backup_path`; `rollback_dag(dag_id)` restores the `.bak` and drops it (returns `rolled_back`); `delete` (→ purge_dag/undeploy) removes the backup too; `deploy_dag` returns `backed_up` (= the file pre-existed → a rollback target exists). Handler `dags/rollback` (→ `rollback_dag`); Undeploy reuses `dags/delete`→`purge_dag`. Frontend: `IDeployRes.backed_up`/`IRollbackRes`; `rollbackDag`; `DeployBanner` gains **Undeploy** (registered/finished/processing/failed → confirm → `deleteDag`) and **↩ Roll back to previous** (failed + `backedUp` → `rollbackDag` → re-enter the deploy lifecycle); `StudioApp` threads `backedUp` (waiting→failed via a functional update) + `onUndeploy`/`onRollback` (cancel poll, destructive-confirm undeploy, re-poll after rollback). Tests: +4 deploy (backup-only-on-overwrite + rollback restore/no-op, delete clears backup, `backed_up` flag, airflowignore), +1 handler (rollback endpoint). Verified: 111 server, 57 jest, tsc/eslint 0, prettier clean, dev build OK; **live end-to-end against `apache/airflow:3.0.2`** (deploy v1 backed_up False → re-deploy v2 backed_up True + `.bak` created → rollback restores v1). PRD §7 + §6.5.5 + §15.6 (wireframe + Built note) flipped to ✅. Ran a 3-dimension adversarial review (7 agents) → **2 confirmed (doc-level), both fixed**: "previous *working* version" overpromised (rollback restores the previous *deployed* version, which can itself be broken) → reworded §7 + the deploy.py comment to "deployed"; the §15.6 wireframe/Built note didn't show Undeploy on the registered/finished/processing phases → added them. (2 refuted: failed-undeploy→error and undeploy→idle are intended fail-safe/feedback choices, not defects — the Manager Delete is the documented idempotent fallback.)

- [x] 2026-06-20 — Per-node **common params** wiring (§6.1.3) — full stack; makes the just-shipped sensors' poke config (and per-task retries/retry_delay/depends_on_past for all ops) editable + emitted. IR: `IAfdagNode.common` slot (graph `irToFlow`/`flowToIR` round-trip it, omitting an empty `common`; back-compatible — absent on older `.afdag`). Server: `client_view` ships `commonParams`; codegen `_node_common(node, op)` builds the `common` dict restricted to the op's declared `common_params` (deterministic order), coercing types and turning `retry_delay` into a `timedelta(seconds=…)` via a new `_Raw` sentinel (`_pyrepr` emits it verbatim); `_render` passes it to `{{ common | pyargs }}`. The 4 TaskFlow-native decorators (bash/branch/python_task/shortcircuit) got a `{% if common %}, {{ common | pyargs }}{% endif %}` slot (operator-type templates already had one); **output-preserving when no common is set** (existing tests unchanged). Form: a static `COMMON_PARAM_DEFS` (the 6 Airflow built-ins: retries/retry_delay/depends_on_past + sensor mode[enum]/poke_interval/timeout) drives a nested **"Common settings"** RJSF fieldset (`__common__`) ordered after the op params; `nodeToFormData`/`formDataToNode` thread `node.common`, keeping only explicitly-set values (a `false` toggle = the default → omitted, so unset inherits the DAG default). `NodeTab` passes `common` through `updateNode` (already merges arbitrary patch keys). CSS: the fieldset renders as a `--jp-*` section divider (legend only on the titled common section, not the untitled root). Tests: +3 codegen (emit native+operator, declared-only/skip-blank, no-common-unchanged), +1 registry (commonParams shipped), +3 jest (forms common section round-trip + false/blank omission + no-section op; graph node.common round-trip). Verified: 106 server, 57 jest, tsc/eslint/stylelint 0, prettier clean, dev build OK. The emitted kwargs (retries/retry_delay/depends_on_past on @task decorators + operators; mode/poke_interval/timeout on sensors) are standard Airflow-3 BaseOperator/BaseSensorOperator args. PRD §6.1.3 NODE common section ✅, §15.3 wireframe + §6.2.1 sensor-common-params note updated.

- [x] 2026-06-20 — (P2/v1.2; §6.2.1) **Cloud/Kubernetes gated ops** as registry YAML (catalogue 14→18, the last roadmap tier). `kubernetes_pod.yaml` (`KubernetesPodOperator`, category Kubernetes, cncf-kubernetes; starter params image*/name/namespace/cmds/arguments[json]/env_vars[json]/on_finish_action/kubernetes_conn_id — advanced surface volumes/secrets/affinity deferred; **description carries the ACE blast-radius warning + the cluster/executor prereq Studio can't check**), `s3_key_sensor.yaml` (`S3KeySensor`, Sensors, amazon; bucket_name/bucket_key/aws_conn_id), `gcs_object_sensor.yaml` (`GCSObjectExistenceSensor`, Sensors, google; bucket/object/google_cloud_conn_id — the canonical non-deprecated conn param), `bigquery_insert_job.yaml` (`BigQueryInsertJobOperator`, category Cloud, google; configuration[json]*/project_id/location/gcp_conn_id). **Verified import paths + classes + every emitted kwarg against the EXACT live provider versions** (cncf-kubernetes 10.5.0, amazon 9.8.0, google 15.1.0) via wheel inspection (incl. the new `operators.pod` not the legacy module, `on_finish_action` not `is_delete_operator_pod`, GCS `google_cloud_conn_id`). **Codegen**: KPO/BigQuery have many optional `{% if %}` kwargs, so an omitted *middle* optional left a stray blank line inside the call (only blanks before `)` were stripped). Fixed by blank-stripping **operator-instance blocks at render time** (`is_operator` branch in `_render`) — surgical: those blocks hold no user code, so `_tidy` stays untouched and a code node's user-authored body keeps its blank lines (regression test added). **Live end-to-end against `apache/airflow:3.0.2`**: all 4 annotate `available` and a deploy using KPO+S3+BigQuery succeeds (gate doesn't false-block); against a standard-only index → `missing-provider` + pip hint. Palette adds Kubernetes/Cloud categories + dims when absent — no frontend change. Tests: +1 registry, +3 codegen (P2 render, operator-block strip, code-node-body preserved). Verified: 102 server tests pass. Ran a 3-dimension adversarial review (4 agents) → **0 confirmed findings** (its lone finding described the `_tidy` comma-rule I'd already replaced with the render-time strip; the verifier rendered real DAGs against the shipped code and confirmed correct). PRD §6.2.1 intro + P2 roadmap rows + KPO-specifics bullet ✅, §15.1 + §15.7 + §5 v1.2 notes updated.

- [x] 2026-06-20 — (P1; §6.2.1) **Gated provider ops** as registry YAML (the first non-standard-provider ops, riding the gating mechanism). `http.yaml` (`HttpOperator`, category HTTP, `providers-http`; params http_conn_id/endpoint/method/data/headers — steers to a Connection, not raw URLs), `sql.yaml` (`SQLExecuteQueryOperator`, category SQL, `providers-common-sql`; conn_id/sql), `sql_sensor.yaml` (`SqlSensor`, category Sensors, `providers-common-sql`; conn_id/sql + sensor common_params). All operator-style, YAML-only (no React/Python). **Verified import paths + classes + every emitted constructor kwarg against the EXACT live provider versions** (http 5.3.0, common-sql 1.27.1) via wheel inspection. Codegen renders valid Airflow-3 Python (correct `airflow.providers.{http,common.sql}.*` imports, no Airflow-2/`SimpleHttpOperator` paths; optional http params emitted only when set). **Live end-to-end against `apache/airflow:3.0.2`**: the 3 ops annotate `available` (providers installed) and a deploy using all 3 succeeds — the gate doesn't false-block installed gated providers; against a standard-only index they annotate `missing-provider` with the right pip hint. Palette adds HTTP/SQL categories + dims them when the provider is absent — no frontend change. Tests: +1 registry (P1 ids/providers/no-AF2-paths/Sensors+common_params), +2 codegen (render + http optional-param emission). PRD §6.2.1 intro + roadmap P1 rows ✅, §15.1 + §15.7 notes updated. Then ran a 3-dimension adversarial review (8 agents) → 5 findings deduped to **2 real (low), both fixed**: `http.yaml` placed the optional `http_conn_id` before the required params, so clearing it left a stray blank line inside the generated `HttpOperator(...)` (the other ops put optionals last) → reordered to required-first + regression test; the §15.1 palette wireframe still showed HTTP as `(P1 🔭)` and omitted SQL → flipped to `(P1 ✅)` + surfaced HTTP/SQL/SqlSensor. Verified: 98 server tests pass.

- [x] 2026-06-20 — (P1 prerequisite; §6.2.1/§15.7) **Provider-availability gating** — full stack. Server: `client.list_providers` (`GET /api/v2/providers`) + `client.version` (`GET /api/v2/version`); new **`providers.py`** — `get_target_index(force)` returns `{providers:{pkg:ver}, airflow_version}` cached with a **60 s TTL + force**; pure `availability(provider, airflow_min_version, index)` → `available|missing-provider|version-too-old|unknown`; `annotate_view`/`annotated_operators` tag each `client_view()` entry (+ `pipInstall` hint); `provider_block_errors(ir, index)`. `OperatorsHandler` serves the annotated view with `?refresh=1`. `deploy_dag` **hard-fails** on a missing/too-old provider (plain-language `errors`, no write) AFTER validation. **Degrades gracefully**: target unreachable → index `None` → everything `unknown` (shown, never blocked) and the deploy gate is a no-op (`/importErrors` stays authoritative); standard/unspecified provider is always available so all P0 ops never gate. Frontend: `IOperatorDef` + `availability`/`pipInstall`; `Palette` dims unavailable ops (opacity + an `ⓘ` glyph — non-color-only — + a pip/version `title`, still addable) and a **⟳ refresh** button (`loadOperators(true)`→`listOperators(refresh=1)`); `InfoTab` availability note; `StudioApp.refreshOperators`; `handler.ts`/`operators.ts` refresh plumbing; CSS. Tests: +2 client, +10 `test_providers.py` (availability/version-compare/annotate/block/cache-TTL+force/None-on-error), +2 handler FakeClient methods + annotation assertion + a `reset_cache` fixture. Verified: **95 server tests**, 53 jest, tsc/eslint/stylelint 0, prettier clean, dev build OK; **live end-to-end against `apache/airflow:3.0.2`** — all current ops annotated `available`; a deploy using a genuinely-missing provider (databricks) hard-fails before write, a standard-only deploy proceeds. PRD §15.7 flipped 🔭→✅ (+as-built note), §15.5 INFO note, §6.2.1 gating bullet marked built. Then ran a 3-dimension adversarial review (5 agents) and **fixed the 1 confirmed HIGH**: the palette ⟳ routed a refresh failure into `opsError`, whose top-level fatal render gate would tear down the whole editor (unmounting the refresh button → no recovery) on a transient blip — now the fatal screen is gated on first-load only (`opsError && !opsLoaded`) and refresh failures surface non-destructively via `showErrorMessage` (last-good operators stay). (1 finding refuted: version-too-old copy omitting a deploy-block note is a non-defect — behavior is correct.)

- [x] 2026-06-19 — (P0; §6.2.1) Add the next standard-provider operators as **registry YAML only** (no React/Python/codegen change). Flow Control: `shortcircuit.yaml` (`ShortCircuitOperator`, native `@task.short_circuit`, reuses the code-node form) + `latest_only.yaml` (`LatestOnlyOperator`, operator-style, zero params). New **Sensors** category (4 ops, all operator-style): `file_sensor.yaml` (`FileSensor` — filepath + optional fs_conn_id), `external_task_sensor.yaml` (`ExternalTaskSensor` — external_dag_id + optional external_task_id; description caveats the same-logical-date default), `datetime_sensor.yaml` (`DateTimeSensor` — target_time), `timedelta_sensor.yaml` (`TimeDeltaSensor` — delta_seconds → `timedelta(seconds=…)`, reusing the pinned timedelta import). Each sensor declares the sensor `common_params: [retries, retry_delay, depends_on_past, mode, poke_interval, timeout]`, establishing the mode/poke_interval/timeout set (inert until the per-node `common` wiring lands — codegen passes `common={}` today, same as the existing operator set, so sensors ride Airflow's defaults). Palette groups by category dynamically → the new Flow Control ops + Sensors group appear with no React change. **Authoritatively verified all 6 import paths, classes, AND emitted constructor kwargs against the real `apache-airflow-providers-standard` wheel** (module inspection; the live dev dag-processor had stalled this session — only HELLO registers — so a DagBag round-trip wasn't possible). Codegen: `generate_dag` renders valid Airflow-3 Python for every op (ast.parse+compile pass; correct `airflow.providers.standard.*` imports, no Airflow-2 paths). Tests: +3 registry (P0 ids present, sensors are standard-provider/no AF2 paths, sensor common_params + Sensors category, client_view ships a sensor), +4 codegen (sensors render as operators, optional param emitted only when set, shortcircuit `@task.short_circuit`, latest_only operator). PRD §6.2.1 intro + roadmap P0 rows flipped to ✅, §15.1 catalogue note updated. Then ran a 3-dimension adversarial review (6 agents) and **fixed all 3 confirmed (low) findings**: `timedelta_sensor` INFO example said `delay_seconds` but the param is `delta_seconds`; `timedelta_sensor` now `int()`-coerces the value (`timedelta(seconds=int(...))`) so a stray stringified `delta_seconds` can't emit importable-but-broken `timedelta(seconds='3600')` (generate_dag reported valid on it); §15.1 palette wireframe SENSORS marker flipped `(P0+ 🔭)`→`(P0 ✅)`. Verified: 85 server tests pass.

- [x] 2026-06-19 — Manager **trigger-with-conf** dialog (PRD §6.6/§15.10; the last "triggers" gap). `ManagerApp.trigger` now calls **`getDagDetails`** (`GET /dags/{id}/details`); a DAG with non-empty `params` opens a new **`TriggerDialog`**, a no-params DAG (or an unreadable details response) keeps the instant bare trigger. Airflow serializes each param as `{value, description, schema}` (a JSON-Schema fragment) — verified the exact shape against live `apache/airflow:3.0.2`. New pure, unit-tested **`triggerForm.ts`** (`classifyParam`/`initialDraft`/`buildConf`) projects params onto typed controls (enum→dropdown, boolean→checkbox, int/number→number input w/ min/max, string+`format:date|date-time`→date/datetime picker, object/array→JSON textarea, else text; infers control from the default's runtime type when `schema:{}`), renders each `description` as inline help, and rebuilds the run `conf` on submit (cleared field → param default, or explicit `null` when nullable; bad JSON/number blocks submit). `logical_date` defaults to now (null); *pick* → UTC ISO. Server: `client.get_dag_details` + `DagDetailsHandler` route `dags/details`. Frontend: `getDagDetails`, `triggerDag(id,conf,logicalDate)`, `IDagParam`/`IDagDetails`. Plain themed React (no RJSF) to keep the manager bundle light. Then ran a 4-dimension adversarial review workflow (14 agents) and **fixed all 8 confirmed findings**: (HIGH) `date-time` conf now normalized to an offset-bearing UTC ISO (Airflow's `date-time` format validation rejects the naked `datetime-local` value); (HIGH) a server-side rejection **keeps the dialog open** with the conf intact and shows the error inline (was: closed + discarded the form — `runTrigger` now returns the error to the caller); (MED) `date-time` defaults reshaped into the local-input format so they populate; (LOW) `integer` rejects non-whole input instead of `parseInt`-truncating; field errors clear on edit; empty *pick* blocked not silently run-now; cleared non-nullable date omits the key (not `''`); pick input got an `aria-label`. (2 findings refuted: the details-fetch→bare-trigger fallback is intended/no regression; error-string i18n-extractability matches the existing manager pattern.) Tests: +1 client, +1 handler (FakeClient), +18 jest (`triggerForm.spec.ts`). Verified: 77 server pass, 53 jest pass (Node-18 `File` polyfill), tsc clean, eslint/stylelint 0, prettier clean on touched files; **end-to-end against live Airflow** (details → trigger-with-conf echoed back validated; pinned logical_date accepted; offset-bearing date-time accepted, naked local value rejected). Also fixed a pre-existing stylelint `comment-empty-line-before` in `base.css`. Wireframe §15.10 flipped 📝→✅ + §15.8 trigger note reconciled.

- [x] 2026-06-17 — Fix the Manager's confirm/logs modal rendering trapped inside the narrow left sidebar (`scope/dialog_box.png`, seen on the Stop-run confirm). Root cause: the `Overlay` (`position: fixed`) was rendered inside the sidebar widget, whose lumino container establishes a containing block (transform/contain), so the fixed backdrop was clipped to the rail instead of the viewport. Fix: `Overlay` now `createPortal`s to `document.body` (react-dom is already a singleton) so it covers the whole window; inner wrapper is `display: contents`; bumped overlay `z-index` to 10000 (above the app chrome). tsc/eslint/prettier clean; dev bundle builds.
- [x] 2026-06-17 — Fix the 5 confirmed findings from the adversarial review of the DAG-lifecycle features. (1, medium) `onStopRun` now cancels any stale poll and **restarts `pollRunState`** after the PATCH, so Stop converges even after the run-poll timeout (previously: dead Stop button → "Stopping…" forever). (2) orphan sweep decoupled from the per-keystroke search — `refresh(pattern, sweep=false)` on the debounced path. (3) `find_orphans` returns `degraded` when a `.afdag` can't be read/parsed; the manager **suppresses the orphan banner** on a degraded sweep so a corrupt-but-present source isn't falsely flagged "deleted". (4) `pollRunState` caps consecutive `getDagRun` errors (`MAX_RUN_POLL_ERRORS=5`) → "lost track, check the Manager" instead of a 10-min stale banner. (5) restored the rename-migration "old DAG retired/purged" confirmation via a persistent `note` field threaded through `runAfterDeploy`→`DeployBanner`. Re-verified: 75 server tests (+2 degraded), 35 jest, tsc + eslint + prettier clean, dev bundle builds.
- [x] 2026-06-17 — **Implement DAG-lifecycle automation** (run-on-deploy · stop-run · delete-on-design-delete). Server: `client.get_dag_run` + `client.set_dag_run_state` (PATCH dagRuns state→failed); `deploy.find_orphans` + `_live_afdag_ids` (afdag_id provenance diff vs Contents root); handlers `dagruns/get`, `dagruns/state`, `dags/orphans` (passes `contents_manager.root_dir`). Frontend: `handler.ts` `getDagRun`/`setDagRunState`/`findOrphans`; `IOrphan`/`IOrphansRes`; DeployBanner `running`/`finished` phases + Stop/Run-again; `StudioApp` auto unpause→trigger→`pollRunState` on `registered` (token-cancelled with the deploy poll) + `onStopRun`; `ManagerApp` stop link on running/queued runs + warn-coloured orphan banner (Undeploy & purge / Keep, session-remembered) wired to `findOrphans` on refresh; `index.ts` subscribes to `contents.fileChanged` (delete `.afdag` → `panel.refresh()`); CSS for orphan banner + danger link. Tests: +2 client, +3 deploy, +4 handlers (73 server pass); tsc clean; 35 jest pass (needs a Node-20 `File` global — Node-18 here needs a polyfill); eslint/prettier clean; dev bundle builds (prod `license-webpack-plugin` crash is pre-existing/env, no deps changed). Wireframes §15.6/§15.8/§15.9/§15.13 flipped 📝→✅; §15.13(A) reconciled to the in-session signal → instant sweep → banner approach (not a separate modal).
- [x] 2026-06-17 — Spec + plan the DAG-lifecycle features the user requested (run-on-deploy, delete-on-design-delete, stop-run; confirm rename already uses the API). Mapped the current server/editor/manager/PRD state via a 7-agent workflow; confirmed rename **already** applies via API (`retire_old_dag`→`delete_dag`/`set_paused`), trigger/pause/delete already API-immediate, and the real gaps are run-on-deploy (deploy makes zero API calls), stop-run (no client/route/UI), and design-file-delete→DAG-delete (unspecced). Captured the Airflow-3 constraints (paused-trigger stays queued → unpause-then-trigger; no run cancel endpoint → PATCH dagRuns state=failed; DELETE refuses while a task runs). Wrote PRD §6.5.4 (run-on-deploy), new §6.5.6 (undeploy & orphan reconciliation), §6.6 stop-run, §8.6/§8.8 endpoints+semantics, §9 destructive-confirm, R13/R14, §13 Q10-12, M7, and wireframes §15.6/§15.8/§15.9 + new §15.13. Implementation queued in Pending above.

- [x] 2026-06-17 — Fix deploy hanging on "waiting for Airflow to pick it up" (`scope/waiting_to_long.png`). Root cause in `deploy.py` `SharedVolumeTarget.write()`: `tempfile.mkstemp()` forces mode 0600, and `os.replace` preserves it, so the deployed `.py` landed owner-only and owned by the JupyterLab `jupyter` uid. On a shared dags volume the Airflow dag-processor runs as a *different* uid (official image's `airflow`, 50000) → it cannot read the file → the DAG never registers → the editor's `pollLifecycle` runs to timeout (neither `registered` nor `failed`, since an unreadable file yields no Python import error). Fix: `os.chmod(tmp, 0o644)` before the atomic rename (new `_DEPLOY_FILE_MODE`; world-readable is the conventional dags-folder mode). Confirmed `.airflowignore` is NOT a cause — Airflow 3.0+ defaults to glob syntax so `*.afdag` ignores only `.afdag` files. Test: `test_deploy.py::test_written_file_is_world_readable`. Also chmod'd the user's already-stuck `HELLO.py` 0600→0644 so it unsticks without re-deploy.
- [x] 2026-06-16 — Actionable deploy error for an unwritable dags folder (`scope/permission.png`; PRD §7). Root cause was environment, not code: the running JupyterLab server had no `AIRFLOW_DAGS_DIR`, so the deploy target fell back to the default `/opt/airflow/dags` (Airflow's own path) that the Jupyter process cannot write → the cryptic `[Errno 13] Permission denied: '/opt/airflow'`. Hardened `deploy.py`: new `SharedVolumeTarget._ensure_root()` (used by `write()` + `ensure_airflowignore()`) creates the dir and, on `OSError` / non-writable target, raises a `DeployError` that names `AIRFLOW_DAGS_DIR` and the devcontainer path + says to restart the server — so the deploy banner shows guidance instead of a raw errno. Test: `test_deploy.py` (+1, uncreatable dir → `DeployError` mentioning `AIRFLOW_DAGS_DIR`). Verified: 63 server tests pass; runtime sanity confirms the message + no recursion. (User-side fix to actually deploy: set `AIRFLOW_DAGS_DIR=/workspace/.devcontainer/airflow-dags` and restart the JupyterLab server — the compose already sets it but this container predates that.)

- [x] 2026-06-16 — Out-of-band **drift detection** (PRD §6.5.3 / §6.5.5 / §15.12 / §8.9). `deploy.py` now stamps `code=sha256:<body-hash>` into the provenance header at write time (`_stamp_code_hash` in `deploy_dag`, hashing everything after the header line — done on the exact bytes written, so it's immune to formatting and needs no codegen change). New `is_drifted(filename)`: a deployed managed file has drifted if its body no longer matches the recorded `code` hash (False when absent / unmanaged / pre-dates the hash → no false alarm). `rename_preflight` now also returns `drifted`. Frontend: `IRenamePreflightRes.drifted`; `StudioApp.onDeploy` adds a second guard after the active-run check — if `drifted`, prompt **Modified outside Studio → Overwrite / Cancel** (overwriting re-stamps a fresh hash). Files: `deploy.py`, `interfaces.ts`, `components/StudioApp.tsx`. Tests: `test_deploy.py` (+5: header carries `code=`, fresh→not-drifted, hand-edit→drifted, no-code-hash→not-drifted, absent/unmanaged→not-drifted; updated the preflight-draft dict for the new `drifted` key). Verified: pytest 19 in test_deploy (full suite green), `tsc` clean, eslint 0, prettier clean. Remaining recovery item: undeploy/rollback (§7).

- [x] 2026-06-16 — Active-run guard on **re-deploy / update** of a deployed DAG (PRD §6.5.5 / §15.12 / R12). A plain Deploy overwrites the same `{dag_id}.py`, which is dangerous mid-run (`LocalDagBundle` runs latest-on-disk, §8.8). `StudioApp.onDeploy` is now async: before deploying it runs the shared dag-state preflight (`renamePreflight(dagId)` → `active_runs`) and, if the DAG is **registered with a run in flight**, blocks with **Cancel / Deploy anyway** (`showDialog`); a preflight failure falls through to deploy. Reuses part B's preflight + dialog UX — **no backend change**; `runDeploy(ir)` (used by both the button and the rename migration) stays guard-free. Distinct from a rename (same `dag_id`, same file/history). PRD: new §6.5.5, wireframe §15.12, risk R12; flagged still-unbuilt drift detection (§6.5.3) + undeploy/rollback (§7). Verified: `tsc` clean, eslint 0, prettier clean. (No new unit test — pure UI orchestration over the already-tested preflight; consistent with the other UI features.)

- [x] 2026-06-16 — Rename a Studio DAG — **part B** (deploy-aware `dag_id` migration), PRD §6.1.8(B) / §8.9 / §15.11 / R11. The DAG-form `dag_id` is now **read-only** (closes the unguarded edit) with a toolbar **Rename DAG id…** action; the part-A button is relabelled **Rename file…**. `StudioApp.onRenameDagId`: validate (new `validateDagId` in `ir.ts` — Python-identifier + keyword check) → `renamePreflight(old)` → branch by state: **draft** (no file, not registered) = just set the id; **deployed-idle** = confirm keep-history vs purge, set the new id, `runDeploy(newIR)`, and once it reaches `registered` the poll lifecycle calls `retireOldDag(old, purge)`; **deployed + active run** = blocked (Cancel / warned override). Order = write-new-then-remove-old (no zero-file gap); a failed/timed-out new deploy clears the pending retire so the old DAG is left intact. Backend: `codegen.py` provenance header now carries **`afdag_id`** (re-association across renames); `deploy.py` gains `rename_preflight` (file_exists/registered/active_runs via `get_dag`+`list_dag_runs`) and `retire_old_dag` (purge=False → remove file + pause, keep history; purge=True → delegates to `purge_dag`); handlers `dags/rename/preflight` (GET) + `dags/retire` (POST). `Inspector` keys `DagTab` on `dag_id` so the read-only field refreshes post-migration. Files: `codegen.py`, `deploy.py`, `handlers.py`, `interfaces.ts`, `handler.ts`, `ir.ts`, `forms.ts`, `components/StudioApp.tsx`, `components/Inspector.tsx`. Tests: `test_deploy.py` (+4: preflight draft / active-runs, retire keep vs purge), `test_codegen.py` (+1: header carries `afdag_id`), `rename.spec.ts` (+4: `validateDagId`). Verified: pytest **57**, jest green, `tsc` clean, eslint 0, prettier clean, full webpack build OK.

- [x] 2026-06-16 — Rename a Studio DAG — **part A** (safe document rename), PRD §6.1.8(A). Added a top-bar **Rename…** button that renames the `.afdag` *file* via the document context — **filesystem-only, no Airflow impact** (the `dag_id` and the deployed `.py` are keyed on `dag_id`/`afdag_id`, not the `.afdag` name; the open widget + `WidgetTracker` follow `context.pathChanged`, already wired). `StudioApp.onRename` prompts via `InputDialog.getText` (pre-selects the stem via `selectionRange`, keeps `.afdag`), normalizes through a new pure `normalizeAfdagFilename` helper in `ir.ts` (trims; rejects empty + path separators; ensures the `.afdag` ext), then calls `context.rename(name)` with `showErrorMessage` on failure. **No new deps** — used `context.rename` (`@jupyterlab/docregistry`) + `InputDialog`/`showErrorMessage` (`@jupyterlab/apputils`), both already present, instead of adding `@jupyterlab/docmanager` for `renameDialog`. Files: `ir.ts`, `components/StudioApp.tsx`. Tests: new `src/__tests__/rename.spec.ts` (5) for the helper. Verified: `tsc` clean, jest **31/31**, eslint 0 (spec is config-ignored), prettier clean on touched files, full webpack build OK. **Part B (the deploy-aware `dag_id` migration) is intentionally deferred** — see the Pending item / §6.1.8(B).

- [x] 2026-06-16 — CODE-tab Python syntax highlighting + line numbers (PRD §6.1.3 / §8.2 / §15.4). `CodeMirrorField` now adds `syntaxHighlighting(jupyterHighlightStyle)` once — JupyterLab's highlight style maps tags to `--jp-mirror-editor-*` CSS vars, so it is theme-aware (light/dark) and colorizes BOTH the CODE preview AND the `code`/`json` node fields in one change. `CodePanel` renders the generated Python in a read-only `CodeMirrorField` (`language="python"`, `readOnly`) instead of `<pre><code>`, gaining the free `lineNumbers()` gutter + selection/scroll. CSS: `.jp-mod-readonly` host class hides the caret + a taller (60vh) scroll box; dropped the now-dead `.jp-afdag-code-pre`. Declared `@codemirror/language` as a direct dep (`^6.0.0`, already in yarn.lock → frozen-safe; `syntaxHighlighting` lives there). Verified: `tsc` clean, eslint 0, stylelint 0, prettier clean, full webpack build OK (confirms `jupyterHighlightStyle` resolves/bundles). No render test added (repo has no RTL infra; CodeMirror needs real layout) — consistent with the other UI features.

- [x] 2026-06-16 — Silence the spurious manager import error (`scope/001-import-error.png`): it was Airflow 3.0.2's OWN bundled example DAG `example_external_task_parent_deferrable.py` importing a test-only `tests_common` module absent from the wheel (`ModuleNotFoundError: No module named 'tests_common'`) — not the extension or a user DAG; the import-errors panel surfaced it correctly. No clean way to drop just the broken example from the stock image, so set `AIRFLOW__CORE__LOAD_EXAMPLES=false` in `.devcontainer/docker-compose.yaml` (+ README) — manager now shows only the mounted demo DAG + deployed DAGs. Takes effect after recreating the Airflow container; reset the `airflow-db` volume to clear the stale records/error.

- [x] 2026-06-15 — Reference-GIF feature mining + PRD wireframes (no app code changed). Pulled the inspiration app's ("Airflow Studio") demo GIFs off Medium's CDN — bypassed the Cloudflare wall via the `?format=json` image-id route, then fetched the open `miro.medium.com` CDN → `design-reference/airflow-studio/` (4 GIFs, ~2550 extracted frames + `extract_frames.py`/`select_keyframes.py`; the article's other images are decorative stock/author photos, noted in the README). Analyzed frame-by-frame + a 7-agent feature-analysis workflow (GIF inventory ×4 + Airflow-operator research + codebase audit → synthesis). Verdict: the editor+manager are far more complete than the GIFs imply — palette, DAG·NODE·INFO·CODE·SAVED tabs, required-field+cycle validation, error badge/node dots, empty-state, minimap, Generate, the Deploy tri-state, note cards, and the whole manager loop are ALL built; **"triggers" already covered** (TriggerDagRunOperator in the registry + the manual one-click Manager trigger) — the only gap is the conf form (already specced §6.6). PRD edits: new **§15 Wireframes** (10 status-marked screens grounded in cited frames + reconciled with code) + **§6.2.1** prioritized operator-catalogue roadmap (P0 ShortCircuit/LatestOnly + first Sensors; P1 provider-availability gating + HTTP/SQL; P2 KubernetesPodOperator + cloud sensors — verified Airflow-3 class/import/provider names, impact, KPO starter-param/gating analysis, and the gating mechanism) + §5 pointers. `CLAUDE.md`: agents must keep §15 wireframes in sync on any UI change. Follow-ups queued in Pending.

- [x] 2026-06-15 — Annotation/note cards (PRD §6.1.7/§8.3). IR gains an optional, separate `notes[]` (`IAfdagNote {id,text,position,size?}`) kept OUT of `nodes[]`/`edges[]` so codegen/validation/cycle-check are untouched (verified: notes never enter `ir["nodes"]`). `graph.ts`: `irToFlow` appends note flow nodes (type `noteNode`, marker `op:'__note__'`, width/height) to the shared ReactFlow array; `flowToIR` splits by type → task nodes to `nodes[]`, notes to `notes[]` (size from `node.width ?? measured`, omits the `notes` key when empty); `isNoteNode`/`NOTE_OP`/`DEFAULT_NOTE_SIZE` exports. New `NoteNode.tsx`: resizable (`NodeResizer`) sticky card, NO handles (can't be wired), a drag-handle bar (textarea has `nodrag`/`nowheel` so it stays editable) + hover × (reuses `deleteNode`); `editorContext` gains `updateNoteText`. `StudioApp`: registers `noteNode`, `addNote`, derives `taskNodes` (excludes notes from error badge/count/`clientErrors`/`hasCycle`/deploy-gate/inspector selection); `onNodeClick` ignores notes. `Palette` gains "+ Add note". CSS for note + add-note button. Tests: `graph.spec.ts` (+2 — note round-trip incl. size, kept out of `nodes[]`; `notes` key omitted when none). Confirmed via xyflow source that `NodeResizer` sets `node.width/height` (setAttributes), so size persists. Verified: `tsc` clean, jest 18/18, eslint 0, prettier clean, stylelint 0, full webpack build OK. (Markdown rendering deferred — plain text for now.)

- [x] 2026-06-15 — INFO tab + contextual field help (M0.5, PRD §6.1.3/§6.2/§7). New registry YAML fields on all 5 operators: operator-level `description`/`docs_url`/`example` + per-param `help` (plain-language). Server: `registry.py` adds `help` to `_CLIENT_PARAM_FIELDS` + a `_CLIENT_DOC_FIELDS` map so `client_view()` ships `description`/`docsUrl`/`example`/`provider`/`airflowMinVersion` (provider+version now exposed for the INFO "what this needs" line; imports/templates still withheld). TS `IOperatorDef`/`IOperatorParam` extended (`operators.ts` casts straight through, no mapping change). `forms.ts` `paramSchema` sets RJSF `description` from `help` → inline field help renders under each NODE field (`.field-description` already styled). New `InfoTab.tsx` (operator desc/required+optional inputs/provider/example/docs↗, or DAG-concepts when nothing selected) + 5th inspector tab (DAG·NODE·INFO·CODE·SAVED); all text React-escaped plain text (registry is user-extensible → no raw HTML), docs link `rel=noopener`. CSS for the INFO surface. Tests: `test_registry.py` (+doc-fields test; updated the hidden-fields assertion since `provider` is now shipped), `forms.spec.ts` (+help→description). Verified: 52/52 pytest, `tsc` clean, jest 16/16, eslint 0, prettier clean, stylelint 0, full webpack build OK. (Note: `docs_url` values are best-effort official Airflow URLs — trivially editable in YAML.)

- [x] 2026-06-15 — Collapsible side panels (M0.5, PRD §6.1.6/§7). `StudioApp` holds ephemeral `leftCollapsed`/`rightCollapsed` + `toggleLeft`/`toggleRight`; `Palette` and `Inspector` take `collapsed`/`onToggle` and, when collapsed, render a ~30px rail (expand chevron `«`/`»` with `aria-label`+`aria-expanded` + rotated label) — early-return placed AFTER all hooks. Inspector tabs wrapped in a new `.jp-afdag-inspector-head` with the collapse chevron beside the tablist (border moved to the head). CSS: `flex-basis` 0.12s transition, `.jp-mod-collapsed` rail, `.jp-afdag-collapse-btn`, `.jp-afdag-rail-label` (vertical). The width change is internal (Lumino widget doesn't resize → `resized` signal silent), so a `setTimeout(fitView, 160)` effect keyed on the collapse state re-fits the canvas after the transition. State is ephemeral (not in IR — would dirty the doc). Files: `StudioApp.tsx`, `Palette.tsx`, `Inspector.tsx`, `afdag.css`. Verified: `tsc` clean, eslint 0 err, prettier clean, stylelint 0 (also fixed a pre-existing `no-descending-specificity` on `.jp-afdag-node-del:hover` from the graph-editing task — stylelint hadn't been run then), full webpack build OK.

- [x] 2026-06-15 — Designer graph editing + rounded-corner edges (M0.5, PRD §6.1.1). New `AfdagEdge` custom edge = `getSmoothStepPath`(borderRadius 8) + `ArrowClosed`, registered as `edgeTypes`/`defaultEdgeOptions` (+ `ConnectionLineType.SmoothStep` drag preview); edges themed via `--jp-*` (selected/hover→brand). Deletion: **node** ✕ in-card hover/focus/selected button + NODE-tab "Delete task" + keyboard (`deleteKeyCode=['Delete','Backspace']`), node delete cascades to incident edges; **edge** ✕ at midpoint (`EdgeLabelRenderer`, shown when selected) + select+Delete; shared `deleteNode`/`deleteEdge` via new `EditorActionsContext` (stable, so memoized nodes don't churn). Reconnect: `onReconnect`+`reconnectEdge`, edges `reconnectable`; invalid/empty drop snaps back (no drop-to-delete — deletion stays explicit). New `graph.canConnect` (no self-loop, no dup `(source,target)`) shared by `onConnect`+`isValidConnection`; `onNodesDelete` clears stale `selectedId`. Files: `graph.ts`, `StudioApp.tsx`, new `AfdagEdge.tsx`+`editorContext.ts`, `AfdagNode.tsx`, `NodeTab.tsx`, `afdag.css`. Tests: new `graph.spec.ts` (8) — edge mapping/type/reconnectable/marker, flowToIR strip, edge-delete keeps nodes, canConnect, hasCycle. Verified: `tsc` clean, jest 15/15 (graph+forms, via Node-18 `File` polyfill), eslint 0 err, prettier clean. (Folded in the separate "rounded-corner arrow edges" task. Two as-built deviations from the spec, reconciled into PRD §6.1.1: edge ✕ on selection not hover — `EdgeLabelRenderer` is a portal so cross-portal CSS hover is unreliable; reconnect snaps back instead of drop-to-delete to avoid accidental loss.)

- [x] 2026-06-15 — Fix DAG `tags` field rendering: it was a JSON-Schema `array` (`forms.ts`) → RJSF's default array widget (Add/remove/move buttons laid out with Bootstrap grid classes) rendered as unstyled, misaligned controls since the editor loads no Bootstrap CSS. Now edited as a **comma-separated text input** (same text-not-widget pattern as `params`/`default_args`): schema `type:'string'` + description/placeholder; `dagToFormData` joins `', '`, `formDataToDag` uses new `parseTags` helper (split/trim/de-dup, empty→`[]`, array back-compat). IR `dag.tags` stays `string[]` so codegen is unaffected. Tests updated (`forms.spec.ts`, 7/7). `tsc` clean. (Note: jest can't init in this Node 18 sandbox — `@jupyterlab/testing` jsdom needs the Node 20+ `File` global — pre-existing, affects all tests; verified via a `File` polyfill.)

- [x] 2026-06-15 — Specced 5 designer features into `docs/PRD.md` (none were previously covered): node delete + edge disconnect/reconnect (§6.1.1, shared `isValidConnection` incl. required duplicate-`(source,target)` guard for the deterministic `e_src__tgt` id), rounded-corner `smoothstep` arrow edges (§6.1.1), collapsible palette/inspector with `fitView` remeasure (§6.1.6/§7), INFO tab + per-field contextual help from new registry `description`/`docs_url`/param `help` fields rendered as plain text/sanitized (§6.1.3/§6.2/§7/§9/AppA), and post-MVP annotation/note cards as a separate IR `notes[]` array kept out of codegen/validation (§6.1.7/§8.3/AppB). Added milestone M0.5; the 5 implementation tasks are in Pending. Grounded by direct reads + a 6-agent understanding workflow.

- [x] 2026-06-15 — Bump `requires-python` to `>=3.9` in `pyproject.toml` (Airflow 3 needs 3.9+); dropped the now-stale `Python :: 3.8` classifier.

- [x] 2026-06-15 — Debounce IR→model commit to node drag-stop (`StudioApp.tsx`): a `draggingRef` (set on `onNodeDragStart`, cleared on `onNodeDragStop`) gates the persist effect so a node drag commits **once** at drag-stop instead of every ReactFlow frame; extracted a `commit()` that reads the latest graph via a ref. Non-drag edits still commit immediately; selection-only churn was already deduped by the serialized-IR compare.

- [x] 2026-06-15 — Manager expansion: fixed `list_dags` (`only_active`→`exclude_stale`, form-exploded params, `dag_id_pattern` search); added client `list_task_instances`/`get_task_logs` (list→text normalise)/`clear_task_instances`/`delete_dag` + `deploy.purge_dag` (remove `.py` first, then `DELETE /dags/{id}`); routes `taskinstances`, `taskinstances/logs`, `taskinstances/clear`, `dags/delete`. Refactored `AirflowPanel` into a thin shell + functional `ManagerApp`: dag_id search, import-errors panel + per-DAG `has_import_errors` badge, run→task-instance drill-down, log viewer modal, clear/retry (dry-run preview→confirm), delete (confirm→purge). Tests: client (exclude_stale, clear payload, logs normalise, delete) + 4 endpoints + `listTaskInstances` handler. Follow-up: tiered visibility-gated auto-refresh (PRD §6.5.4) and trigger-conf form from `/dags/{id}/details` not yet built.
- [x] 2026-06-15 — Deploy lifecycle (PRD §6.5.4): `AirflowClient.deploy_status(dag_id, filename)` composes `GET /importErrors` (basename-matched) + `GET /dags/{id}` (404 = not-yet) into a tri-state (`registered`/`failed`/`processing`); `list_import_errors`; `deploy/status` + `importerrors` routes. Frontend `deployStatus`/`listImportErrors` handlers; `StudioApp` runs the Writing→Waiting→(Registered|Failed|Processing) state machine with bounded backoff polling (2s→8s, 3-min timeout, cancel-on-unmount/dismiss); `DeployBanner` renders the tri-state with Unpause&trigger / traceback expander / Keep-waiting / Dismiss. Tests: client deploy_status (3 states), deploy/status + importerrors endpoints, deployStatus handler.
- [x] 2026-06-14 — Server `POST validate` + `POST deploy`: `validation.py` runs the full Appendix E pipeline (stages 1–6 from codegen + best-effort black format + stage 7 `DagBag` import in an isolated, secret-scrubbed, timed subprocess that reports `skipped` when Airflow is absent); `deploy.py` `SharedVolumeTarget` does the atomic co-located-temp + `os.replace` write with provenance-header collision safety (refuses to clobber non-Studio files), path-traversal guards, `list`/`verify`/`delete`, and `.airflowignore`; `deploy_dag` validates-then-writes. `ValidateHandler`/`DeployHandler` routes; `AIRFLOW_DAGS_DIR` config (+ devcontainer env). Frontend `validateDag`/`deployDag` + a top-bar Deploy button with status (disabled on client errors). Tests: validation, deploy, endpoints, deploy handler. Note: subprocess sandboxing is timeout + env-scrub only (full resource/network limits = hardening follow-up); the post-deploy import poll (tri-state UI) is the next task.
- [x] 2026-06-14 — Registry-driven RJSF forms + four inspector tabs (DAG / NODE / CODE / SAVED): `forms.ts` builds JSON-Schema/uiSchema from the registry; `AfdagForm` (RJSF + ajv8) with custom `code`/`json`/`schedule` widgets; `code`/`json` fields use an embedded CodeMirror 6 editor (`CodeMirrorField`); NODE form is registry-generated, DAG form fixed; CODE tab gains Generate-DAG + client/server validation panel; SAVED tab lists workspace `.afdag` docs via Contents API (services threaded index→factory→widget→app). Deps: @rjsf/* + @codemirror/*. Tests: `forms.spec.ts`. Follow-ups: per-node common params (retries/retry_delay/depends_on_past) need an IR slot before wiring into the NODE form + codegen `common`; SAVED deploy-status marking deferred to manager correlation.
- [x] 2026-06-14 — Server `POST generate`: IR → Airflow 3.x TaskFlow Python via `codegen.py` (Jinja2 `autoescape=False` + `pyrepr`/`pyargs`, registry templates, Kahn topo order, Appendix E validation: identifiers/cycle/`ast.parse`/`compile`, provenance header); `GenerateHandler` route; frontend `generateDag` + `CodePanel` (debounced) wired as a Build/Code tab in the inspector; tests added (codegen, endpoint, handler)
- [x] 2026-06-14 — Server `GET operators`: YAML operator registry (`jupyterlab_airflow/operators/*.yaml` + `registry.py` with mtime hot-reload + `AIRFLOW_OPERATORS_DIR` override) served via `OperatorsHandler`; `src/operators.ts` now fetches/caches the registry (`loadOperators`) instead of a hardcoded list; tests added (registry, endpoint, loader)
- [x] 2026-06-13 — Establish this `TODO.md` task-history convention (documented in `CLAUDE.md`)
- [x] 2026-06-13 — Add `CLAUDE.md` (commands + architecture for future instances)
- [x] 2026-06-13 — Scaffold the `.afdag` document widget: IR/model/factory/widget + ReactFlow editor + two-plugin wiring; `tsc` clean and jest passing
- [x] 2026-06-13 — Write the product PRD (`docs/PRD.md`) with grounded research + adversarial review
